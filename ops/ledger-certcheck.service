# 서버 위치 /etc/systemd/system/ledger-certcheck.service. 정본은 저장소 ops/ (ADR-051)
[Unit]
Description=TLS certificate expiry check
After=nginx.service

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/ledger-certcheck
