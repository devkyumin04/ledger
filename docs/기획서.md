# Ledger V3.0 기획서

## 한 줄 요약

영수증·결제내역 캡처만 올리면 AI가 등록해주고 지도로 소비 위치를 보는 가계부 + 가족·연인·모임용 1/N 자동 정산 공동 가계부. 프론트/백 REST API 완전 분리, PWA 적용.

---

## AI 자동 분석 파이프라인

### 1단계 — 텍스트 추출

네이버 Clova **일반 OCR** 사용 (영수증 특화 OCR 대신).

> 뱅킹앱·결제알림 캡처 비중이 높을 것으로 판단. 월 100회 무료.

### 2단계 — 파싱

1. 정규표현식으로 금액 / 날짜 / 결제대상을 먼저 시도
2. 실패했을 때만 LLM 보조 호출

은행앱별 패턴은 실제로 부딪히며 하나씩 등록해나갈 계획.

### 3단계 — 분류

상호명 임베딩(코사인 유사도)으로 본인 과거 내역을 검색한다.

- **과거 내역이 있으면** → 그때 쓴 카테고리·메모를 추천 (LLM 미호출)
- **없으면** → LLM에 유저 카테고리 전체 목록을 주고 기존 중 선택하거나 신규 제안하게 함

신규 카테고리 제안 시 확인창(`'구독료' 만들까요?`)을 띄우고, 승인하면 생성 / 거절하면 미분류로 등록한다. 기존 카테고리와 문자열·임베딩 유사도를 체크해 중복 제안을 막고, `미분류`는 시스템 예약어라 제안 자체를 차단한다.

### 원칙

- **AI 결과는 바로 저장하지 않는다.** 유저가 확인·수정한 뒤 저장 (돈 관련이라 100% 자동 저장은 위험)
- 개인은 유저 단위, 공동은 그룹 단위로 학습 프로필을 분리. 공동 분석 전에 멤버 여부 확인

### 실패 대응

| 상황 | 처리 |
|---|---|
| 과거 내역 없음 | 임베딩 건너뛰고 LLM 직행 |
| OCR·LLM 실패 | "직접 입력해주세요"로 수동 입력 화면 이동 |
| 확인창 미응답 | 정리 스케줄러가 처리 |

---

## 공동 가계부 / 정산

### 정산 사이클

- **매월 정산형** — 월간 초기화
- **자유 정산형** — 수동 초기화

### 1/N 계산

```
인당 부담액 = 총지출 ÷ 인원
개인 수지   = 실제 낸 금액 - 부담액
             (+) 받을 사람 / (-) 낼 사람
```

소수점은 내림 처리하고, 자투리는 제일 많이 받을 사람이 덜 받아 합을 0으로 맞춘다.

### 그리디 매칭

최대 채권자와 최대 채무자를 1:1로 우선 매칭해 송금 횟수를 최소화한다.
최소 송금 횟수를 보장하는 알고리즘은 아니지만 2~10명 규모에선 결과가 거의 동일하다. 완전탐색은 백로그.

### 방장 권한

초대 / 방 정보 수정 / 방 삭제 / 정산 켜고 끄기 / 강퇴 / 방장 넘기기.
방장이 나가면 **가장 먼저 들어온 멤버에게 자동 위임**된다.

### 제한 및 정책

- 1인 최대 10개 방, 방당 최대 50명
- 정산이 안 끝난 돈이 남아 있어도 강퇴·탈퇴를 시스템에서 막지 않는다. 나간 사람의 지출은 "그룹에 없는 사용자"로 표시
- **로그 박제** — 공동 지출 수정·삭제 시 로그 테이블에 강제 기록. 방장 포함 아무도 삭제할 수 없다
- 지난 달·보관된 과거 데이터는 방장도 수정 불가

---

## 지도 / 카테고리 / 통계 / PWA

### 카카오맵

- 핀 이모지는 카테고리 이모지를 재사용 (없으면 기본 핀)
- 위치는 선택 입력 (카카오맵 장소검색)
- 축소 시 클러스터링, 확대 시 이모지 핀
- 개인 지도와 공동 지도를 분리. 별도 필터 탭 없이 현재 장부 + 월 선택 UI를 따름

### 카테고리

- **대분류 - 소분류 2계층만.** 더 깊으면 입력이 귀찮고 AI 분류 정확도가 떨어짐
- 이모지는 생성 시 한 번 선택
- 자주 쓰는 카테고리명엔 서버 고정 목록으로 기본 이모지를 추천

### 소셜 자극

유저 ID 검색으로 '공개' 가계부를 열람할 수 있다 (위치 정보도 함께 노출됨).

### 통계

- 이달 카테고리별 파이차트
- 흑자/적자 비율 (수입 0원일 때 나누기 처리 필요)
- 최근 6개월 개인·공동 추이 그래프

### PWA

모바일 우선 화면. 설치 조건상 HTTPS 필수.

---

## 인증 / 보안

### 토큰

- JWT **Access 15분 / Refresh 14일**
- Refresh는 httpOnly + Secure 쿠키 + 서버 저장 (로그아웃·비번변경 시 즉시 무효화)
- **Refresh 로테이션 + 재사용 탐지** — 무효화된 Refresh로 재발급을 시도하면 탈취로 간주하고 해당 계정의 전 세션을 강제 로그아웃

### 로그인

- 소셜 로그인은 **배포 후** 카카오 1사부터 (개발자 콘솔에 배포 URL 등록이 선행). 네이버·구글은 이후. 로그인 후엔 자체 JWT 발급
- 동일 이메일의 기존 계정이 있어도 **자동 연동하지 않고 별도 안내**
- 로그인 실패 5회 초과 시 30분 잠금, 성공 시 카운트 초기화
- 로그인 빈도 자체는 Refresh 14일로 낮춤(기기당 2주 1회). 지문·FaceID는 패스키(WebAuthn, 웹·PWA 공통 표준)로 비밀번호 대체 — Sprint 5 이후

### 비밀번호 재설정

- 이메일 링크 30분 유효, 1회용
- 같은 이메일로 1시간에 3회를 초과하면 발송 중단

### 봇·남용 방지

- 회원가입·비번재설정에 reCAPTCHA
- 같은 IP에서 단시간 다수 가입 차단, 가입 IP 기록
- 유저 검색 기능도 호출 횟수 제한

### API 호출 제한

Redis 카운트 + Nginx 이중 구성.

| 대상 | 제한 |
|---|---|
| 일반 API | 유저당 분당 60회 (조정 예정) |
| AI 분석 | 유저당 하루 10회 + 앱 전체 월 사용량 카운터로 Clova 무료 한도 관리 |

초과 시 429. AI 호출 이력은 Redis 카운트가 휘발성이라 DB에 별도 기록한다.

### 업로드

10MB 이하, jpg/jpeg/png만, 파일 시그니처 검사.

### 기타

- 권한 검증 (작성자 본인 확인)
- AI 데이터 격리 (조회 조건에 `USER_NUM` 강제)
- MyBatis `#{}` 바인딩 원칙
- CORS는 프론트 도메인만 허용

---

## 데이터 무결성 / 스케줄러

### 무결성

- 소프트 딜리트 (탈퇴 시 개인정보·아이디는 알아볼 수 없게 처리)
- 금액 0/음수 화면 + 서버 이중 검증
- **낙관적 락**(`version` 컬럼) — 비관적 락 대신. 충돌 빈도가 낮은 실사용 패턴을 고려
- **트랜잭션 분리** — AI 분석 몇 초 동안 DB 커넥션을 붙잡지 않도록 임시 저장과 최종 저장을 분리
- 예외 처리를 한곳에 모아 응답 형식 통일

### 스케줄러

| 주기 | 작업 |
|---|---|
| 30분 | 15분 지난 '처리중' 분석 → 실패 처리 + S3 원본 삭제 |
| 하루 1회 | 미연결 S3 이미지(1일 이상) 삭제 |
| — | 7일 지난 실패·처리중 분석 삭제 |
| — | 만료 인증토큰 삭제 |
| — | 만료·무효 Refresh 삭제 |
| — | 30일 지난 대기 초대장 → 무효 |
| — | 90일 지난 AI 호출 이력 삭제 또는 요약 |

지출·정산·감사로그는 정리 대상에서 제외한다.

---

## 인프라 / 예산

### 구성

EC2 t3.medium(2vCPU 4GB) **한 대**에 Spring Boot + MySQL + 임베딩 모델 + Redis(컨테이너)를 전부 구동. 스왑 대신 여유 메모리 확보를 선택. 이미지는 S3.

- Nginx + Let's Encrypt HTTPS
- GitHub Actions (main 푸시 시 빌드 + 테스트 → 배포 후 자동 배포까지)
- **이중화 없음** — 단일 장애점은 감수(ADR-002). 대신 systemd 자동 재시작 / DB 일 1회 백업 → S3 / UptimeRobot 외부 모니터링으로 "꺼졌을 때"의 피해(다운 몇 분, 데이터 유실)를 막는다. 이중화는 실제 사용자가 생겨 다운타임이 비용이 될 때 재검토
- Swagger, Actuator 헬스체크
- **임베딩** — 가벼운 한국어 문장 임베딩 모델을 서버에서 직접 구동. 벡터는 MySQL JSON에 저장하고 앱에서 코사인 계산 (유저당 수백 개 수준이라 충분)
- **LLM** — Ollama 경량 모델을 서버 내 구동. 정규식 실패·신규 상호일 때만 호출

### 월 예산

| 항목 | 금액 |
|---|---|
| EC2 | 51,000원 |
| EBS 20GB | 2,000원 |
| S3 | 1,500원 |
| 전송 | 2,000원 |
| 잡비 | 1,000원 |
| **AWS 소계** | **약 57,500원** |
| Clova | 0~2,000원 |
| 도메인 (Cloudflare Registrar) | 연 12,000~13,000원 |
| **합계** | **월 60,000~61,000원** |

트래픽에 따라 t3.small / t3.large로 조정할 계획.

---

## 스프린트 계획

| 스프린트 | 범위 |
|---|---|
| **Sprint 0** | 프로젝트 생성, MySQL, Flyway, 환경변수 분리, Git, GitHub Actions 기본 |
| **Sprint 1** | Security + JWT, 회원가입/로그인, 개인 지출·수입 CRUD, 개인 통계, **1차 배포(CD)**, 로그인 실패 잠금, 이메일 비번재설정 |
| **Sprint 2** | Clova OCR, S3 업로드(제한 포함), 정규식 1차 파싱 + LLM 보완, 트랜잭션 분리, 스케줄러. 과거 내역 참고 없이 LLM 단독 분류까지만 만들고 정확도 측정 |
| **Sprint 3** | 임베딩 모델 연동, 벡터 저장·유사도 검색, 카테고리·메모 추천, LLM은 신규 상호 보조로 전환. Sprint 2 정확도와 비교 |
| **Sprint 4** | 방 생성/초대/권한, 정산 알고리즘 + 테스트, 소프트 딜리트·로그 박제, 그룹 통계, AI 파이프라인 공동 적용 |
| **Sprint 5** | 카카오맵 시각화, PWA 전환, API 호출 제한, 전체 점검 |
| **배포 후** | 소셜 로그인(카카오) → 패스키 → 네이버·구글 |

배포를 Sprint 5에서 Sprint 1 끝으로 당김(2026-09-13). 실제 URL에서 폰으로 직접 쓰며 개발해야 다음 할 일이 정확히 보이고,
타임존(ADR-026) 같은 문제는 EC2에 올려야만 드러난다. Nginx + HTTPS + 자동 배포 + SES 샌드박스 해제 신청을 이때 함께.

---

## 테스트 전략

- JUnit5 + Mockito 서비스 단위 테스트
- 확대 순서: 정산 알고리즘 → 금액 검증 → 방장 권한 → 통계 나누기
- 기획 시 손으로 계산한 정산 예시(3명 중 1명 결제, 4명 전원 결제)를 그대로 테스트 케이스로 옮김
- **AI 정확도** — 샘플 30장을 라벨링해 정답률을 측정하고, 임베딩 도입 전/후 같은 샘플로 비교

---

## 백로그

- 정산 완전탐색
- Function Calling 기반 자연어 질의 ("이번 달 카페 얼마?")
- 이상 탐지 알림
- 토큰 블랙리스트
- 로그 테이블 아카이빙 / 파티셔닝
- 데이터가 쌓이면 전용 추출 모델 학습
