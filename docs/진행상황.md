# 진행 상황

## 배경

- Ledger V1을 1인 프로젝트로 완주. V2는 AI OCR·로컬 LLM 분류·지도 클러스터링·1/N 정산 포함
- V3.0은 24시간 상시 운영 실서비스로 배포하고 홍보까지 할 계획
- 2026년 9월 내 Sprint 5까지 목표 (하루 12시간 투입 가능). 현실적 추정은 27~36일 소요라 9월엔 Sprint 2 완료 + 배포 URL이 목표
- 스프린트마다 "깊게 팔 영역" — Sprint 1 트랜잭션·검증 설계 / Sprint 2 OCR 파이프라인 분기 / Sprint 3 임베딩·코사인 유사도 / Sprint 4 그리디 정산·낙관적 락(JUnit까지, 멱등성 후보)

---

## Sprint 1 — 완료 항목 (2026-09-11 기준)

### 회원 · 인증
- 회원가입/로그인 + JWT 인증 — `User`, DTO들, `UserMapper` + XML, `UserService`, `UserController`(`/api/users/*`), BCrypt, Validation, `JwtTokenProvider`, `JwtAuthenticationFilter`, `SecurityConfig`
- 로그인 시 탈퇴(W) 차단 + `user_last_login_at` 갱신
- 인증 응답 정리 — `SecurityConfig` STATELESS + 401 entryPoint. `apiClient.js` 토큰 자동 첨부·401 리다이렉트·204 처리
- 미분류 자동생성 — `signup()`에 `@Transactional` + `PersonalCategoryMapper` 직접 주입. E/I 2개 생성(parent null, emoji null). `PersonalCategory.DEFAULT_CATEGORY_NAME` 상수. 롤백 테스트 확인

### 개인 카테고리 CRUD
- `PersonalCategory`, `CategoryRequest/ResponseDto`, Mapper + XML, Service, Controller(`/api/categories`). 소유권 검증, 소프트 딜리트
- 예약어 차단 — `ReservedCategoryNameException`(400)으로 '미분류' 이름 생성/수정 차단(trim 포함). 미분류 수정·삭제 차단(403)
- `validateParent` — 부모 존재/소유/타입일치/2계층/미분류아님/자기자신아님 + update 시 자식 있으면 소분류로 못 내려감. `InvalidCategoryHierarchyException`(400). 12케이스 QA 통과
- 하위 카테고리 있는 대분류 삭제 거부 (`countChildren`)
- 카테고리 관리 화면 — `views/personal/category.html`, `js/personal/category.js`. 목록/탭(E,I)/모달 기반 CRUD, 2계층 들여쓰기, 미분류 버튼 숨김, 상위 select 제약(자기자신·미분류·타입·자식유무 disabled). **QA 22케이스 통과**

### 공통
- 에러 응답 분리 — API 텍스트 / 페이지 HTML (ADR-012). JSON 파싱 실패·타입 불일치 400 핸들러. 최종 Exception 핸들러는 고정 문구
- `RootController` — `/` → `/views/main.html` 리다이렉트
- 다크모드 — `js/util/theme.js`. OS 설정 기본값 + 토글, localStorage 저장. `common.css` 색상 변수화
- GitHub Actions CI (빌드 + 테스트) 구축. CD는 아직 없음
- `qa/test-category-hierarchy.sh` — 카테고리 12케이스 회귀 스크립트. JUnit 전환 전까지 유지

---

## Sprint 1 — 진행 중: 개인 거래 CRUD

설계 결정은 ADR-006, 008, 009, 023~028 참조.

| 단계 | 상태 |
|---|---|
| 1. `PersonalTransactionRequestDto` / `ResponseDto` | ✅ |
| 2. `PersonalTransaction` 도메인 | ✅ |
| 3. `PersonalTransactionMapper` 인터페이스 + XML (7개 statement) | ✅ |
| 4. `PersonalTransactionService` — 소유권 검증, transType 파생, 낙관적 락 판정 | ✅ |
| 5. Controller(`/api/transactions`) + 예외 3개 409/404/403 등록 | ✅ |
| 6. 카테고리 삭제 시 미분류 이관 (`deleteCategory`에 `@Transactional` + 일괄 UPDATE) | ✅ |
| 7. JVM 기본 타임존 Asia/Seoul 고정 (`@PostConstruct`) | ✅ |
| 8. 거래 관리 화면 (`views/personal/personal-main.html`, `js/personal/transaction.js`) — 위임. 화면 QA 에서 삭제된 참조 문제 발견·수정 (ADR-006 보완) | ✅ |
| 9. API QA — `qa/test-transaction.sh` **52케이스 전체 통과** (2026-09-12) | ✅ |

### QA 결과 요약 (52/52)

- 정상 11 / 실수 19 / 악의적 13 + 응답 body 검사 9
- 검증된 것: `transType` 서버 파생, `version` 0→1→2 연속 수정, 12월 경계,
  조인 값(`categoryName`·`categoryEmoji`), 소유권 403, 낙관적 락 409,
  Mass Assignment 방어(body 에 `userNum`/`transType`/`version` 끼워넣어도 무시)
- 남은 것: 없음
- 7단계 후 회귀 재실행 — `test-transaction.sh` 52 + `test-category-transfer.sh` 35, 전부 통과 (2026-09-13)

### 6단계 이관 QA 결과 (35/35, 2026-09-13)

`qa/test-category-transfer.sh` — 상태코드가 아니라 **부수효과**(거래가 옮겨졌는가)를 검사.
삭제 후 목록을 재조회해 `categoryNum`·`categoryName`·`version`을 확인한다.

- 경계값 — 거래 0건(이관 0행도 정상) / 1건 / 3건 전부 이동
- 타입 — 지출→지출 미분류, 수입→수입 미분류. E·I 공존 시 반대 타입 거래 불변
- 순서 의존 — 이관이 올린 `version` 때문에 옛 version 수정은 409, 새 version은 200.
  이미 삭제된(`use_yn='N'`) 거래는 이관 대상 아님 (DB 직접 확인)
- 부수효과 — 남의 카테고리 403 / 자식 있는 대분류 400 일 때 **이관도 일어나지 않음**
  (검증이 이관보다 앞에 있다는 증명). 미분류 자체 삭제 403 — 이관 목적지 보호
- `defaultCategory` null 체크는 넣지 않음 — 미분류는 `signup()`이 항상 생성(ADR-020)하고,
  깨지는 경로는 SQL 로 유저를 직접 INSERT 한 경우뿐. NPE 가 나도 `@Transactional` 이 롤백

카카오맵 장소 입력은 Sprint 5. 지금은 위치 없이.

---

## Sprint 1 — 완료: 개인 통계 (2026-09-16)

설계 결정은 ADR-038, 039 참조. API 는 하나 — `GET /api/statistics?year=&month=`

| 단계 | 상태 |
|---|---|
| 1. 카테고리별 집계 (봉투 구조) — `findCategoryStats` → `CategoryStatDto`(대분류) ⊃ `SubCategoryStatDto`. 비율은 부모 대비 | ✅ |
| 2. 6개월 추이 — `findMonthlyStats` → `MonthlyStatDto`. 보는 달 포함 롤링 6개, 빈 달 0 채움, balance 서버 계산 | ✅ |
| 3. 조립 — `StatisticsResponseDto`(totalIncome/totalExpense/balance/expenseRatio + expenseList/incomeList/monthlyList), `getStatistics()` | ✅ |
| 4. month 1~12 입구 검증 400 (A안) | ✅ |
| 5. 임시 엔드포인트 `/expense` `/income` `/monthly` 삭제. `test-statistics-monthly.sh` 는 `test-statistics.sh` 에 흡수 | ✅ |
| 6. QA — `qa/test-statistics.sh` **38/38** | ✅ |
| 7. 화면 (`views/personal/statistics.html`, `js/personal/statistics.js`) — 위임. 브라우저 QA 통과 (2026-09-17) | ✅ |

### QA 요약 (38/38)

- 요약 4개 = 봉투 합 = 추이 마지막 원소 (세 출처가 같은 값인지 — 1-a/1-b/1-e)
- 수입 대비 지출 — 수입 0 이면 `null`(계산 불가, 프론트 `-` 표시), 0.4 / 200.0 / 66.7(HALF_UP)
- 봉투 — 대분류 직접 거래 + 소분류가 한 봉투. 자식 비율은 부모 대비 (75.0 + 25.0)
- 빈 목록은 `[]`, 타인 거래 미포함, 삭제 거래 제외, 연도 넘김
- month 13/0/-1 → 400 "허용 범위", 1·12 → 200. 누락·형식 400, 무토큰 401

### 로드맵에 남긴 것

- (정리 커밋에서 해소) 카테고리 집계 조회 1회로 — `buildCategoryStats(rows, type)` 순수 함수화, `getExpenseStats/getIncomeStats` 삭제

---

## Sprint 1 — 진행 중: 1차 배포

설계 결정은 ADR-042 참조. 순서는 **배포할 물건(yml) → 서버 → 자동 배포 → 검문소 → 운영 보강**.
3과 4를 나눈 이유 — 같이 만들면 실패했을 때 배포 문제(SSH·systemd)인지 테스트 문제인지 구분이 안 된다.

| 단계 | 상태 |
|---|---|
| 1. 운영 프로파일 — yml 분리 (직접). 기동 확인: 프로파일 없음 → SQL 로그 없음 / `local` → SQL 로그 찍힘 (2026-09-17) | ✅ |
| 2. EC2 세팅 — 타임존·MySQL·Java·systemd. **프로파일 도입은 여기서 완성** (직접) | 진행 중 — 2-1~2-4 ✅, 2-5 남음 |
| 3. CD 최소판 — jar 전송 + 재시작. 배포 1회 성공 확인 (직접) | |
| 4. 게이트 — `_lib.sh` 비대화 모드(위임) + 워크플로 QA 잡 + `needs:` (직접) | |
| 5. 운영 보강 — HTTPS / DB 백업 크론 → S3 / UptimeRobot / 타임존 3계층 확인 / SES 샌드박스 해제 신청 | |

### 1단계 가이드 — 운영 프로파일 (yml)

- **규칙 하나** — `application-{이름}.yml` 은 그 프로파일이 켜졌을 때만 읽히고, 공통 `application.yml` **위에 덮어씌워진다(merge)**.
  그래서 프로파일 파일엔 **공통과 다른 값만** 적는다. 전체를 복사해 두면 mybatis 설정 하나 바꿀 때 두 파일을 고치게 된다 (ETC 위반)
- 분류 — "맥북과 EC2 에서 값이 달라야 하는가"

| 덩어리 | 달라야 하나 | 위치 |
|---|---|---|
| `spring.datasource` | 아니오 — 세 환경(맥북·CI·EC2) 모두 자기 컴퓨터의 MySQL(`localhost`), 계정·비번은 이미 `${ENV}` | 공통 |
| `mybatis` / `server.port` / `jwt` | 아니오 | 공통 |
| `logging.level.com.kyumin.ledger.mapper: DEBUG` | 예 — 운영에선 사용자 금액·메모가 SQL 바인딩 값으로 로그에 남는다 | `application-local.yml` |

- 할 일 — ① `application-local.yml` 엔 `logging:` 덩어리 3줄만 ② `application.yml` 에서 그 3줄 삭제 ③ STS Run Configuration → Profile 칸에 `local`
  - ③은 저장소가 아니라 STS 에 저장된다. 새 PC·STS 재설치 후 "SQL 로그가 안 보인다"면 Profile 칸부터 확인
- 검증 — `local` 켜고 기동: 로그에 `The following 1 profile is active: "local"` + 조회 시 SQL 찍힘.
  Profile 비우고 기동: `No active profile set` + SQL 안 찍힘 (= 운영의 모습)
- **기본값은 운영에 안전한 쪽** — DEBUG 를 공통에 두고 prod 에서 끄는 구조는 프로파일 지정을 깜빡하면 DEBUG 가 켜진 채 운영된다.
  공통엔 없고 local 에서만 켜면, 깜빡했을 때 "SQL 로그가 안 보임"으로 눈에 띄게·안전하게 실패한다
- `application-prod.yml` 은 **지금 안 만든다** — 넣을 값이 없다 (YAGNI). 파일이 없어도 `prod` 프로파일로 기동된다.
  운영에서만 다른 값이 처음 생길 때(Refresh 쿠키 `Secure`, S3 버킷, SES 발신 주소) 만든다
- DB URL 은 yml 에 그대로 — 세 환경에서 값이 같고 비밀이 아니다. `${DB_URL:...}` 는 DB 를 인스턴스 밖으로 옮길 때 그 한 줄만 바꾸면 된다 (ADR-042)
- CI 는 고칠 것 없음 (프로파일 없이 돈다)

### 2단계 가이드 — EC2 세팅

- **2-1 인스턴스 생성 — ✅ 2026-09-17** (ADR-002 보완). 값은 저장소에 적지 않는다 — IP·키 위치는 저장소 밖 개인 보관처에
  - `ledger-prod` / 서울 / Ubuntu 26.04 LTS arm64 / **t4g.small**(2GB — medium 으로 만들었다가 같은 날 내림) / 30GB gp3 / 크레딧 사양 Standard / 탄력적 IP 연결
  - 키 페어 `ledger-admin`(ED25519, `.pem`) — 내 접속용. CD 용 키는 3단계에서 따로
  - 보안그룹(`launch-wizard-1`) — 22(**내 IP 만**) / 80 / 443. **8080·3306 은 열지 않는다** (앱은 Nginx 뒤, DB 는 localhost 전용)
  - 주의 — 집 IP 가 바뀌면(공유기 재시작 등) SSH 가 타임아웃 난다. 보안그룹 인바운드 22번의 소스를 "내 IP"로 다시 지정
  - SSH 첫 접속 확인 ✅ / 예산 알림 `ledger-monthly` 월 $30 ✅ / 루트 MFA ✅ (폰 OTP, 내장 브라우저 로그인까지 확인). 만든 것 전체와 조작법은 [aws-인프라.md](aws-인프라.md)
  - 접속 배너의 시스템 시각이 **UTC** 였다 — 2-2 에서 OS 타임존부터 바꾸는 이유 (ADR-026)
- **2-2 타임존 → MySQL — ✅ 2026-09-17** (ADR-043 · ADR-044)
  - 순서가 중요 — **OS 타임존 먼저**(`timedatectl set-timezone Asia/Seoul`) → 그다음 MySQL 설치. MySQL 기본 `time_zone=SYSTEM` 이 OS 를 따라간다 (ADR-026 — JDBC `serverTimezone` 옵션으로 덮지 않는다)
  - 커널 업그레이드가 걸려 있으면(`Pending kernel upgrade`) **MySQL 설치 전에 재부팅**한다. 앱이 올라간 뒤엔 재부팅이 곧 서비스 중단이고, 옛 타임존으로 떠 있던 프로세스도 이때 정리된다
  - 실측 — OS `Asia/Seoul (KST, +0900)` · NTP active / MySQL **8.4.11**(`apt-cache policy` Candidate) · `active (running)` · `enabled`(재부팅 자동 기동) / `@@global.time_zone = SYSTEM` · `NOW()` 한국 시각
  - 보안 — `mysql_secure_installation` 실행. 다만 우분투 패키지는 **익명 사용자도 원격 root 도 처음부터 없다**(설치 직후 `mysql.user` 실측). 구멍을 막는 작업이라기보다 확인 작업이다. `root@localhost` 는 `auth_socket` 유지 (ADR-043)
  - `ledger_db` + 앱 전용 계정 `ledger_app`@`localhost` — 맥·CI 와 **같은 이름**, 비밀번호만 새로 만든 값. 테이블은 만들지 않는다 (첫 기동 때 Flyway `V1`)
  - 권한 확인 — `SHOW GRANTS` 가 `USAGE ON *.*` + `ALL PRIVILEGES ON ledger_db.*` 두 줄. `USAGE` 는 "접속만 가능" 이라는 뜻이라 정상
  - collation 은 **명시하지 않는다** — 기본값 `utf8mb4_0900_ai_ci` 가 그대로 세 환경 공통값이 된다 (ADR-044)
  - 메모리 — 설치 직후 `free -h` available 879Mi, 스왑 0. **2-5 에서 앱을 올린 뒤 available 이 300Mi 아래면 1GB 스왑 파일**을 만든다 (그때 판단)
- **2-3 Java 17 · 전용 실행 유저 — ✅ 2026-09-17**
  - `openjdk-17-jre-headless` **17.0.20** (arm64, universe). **JRE 로 충분** — 빌드는 맥·CI 가 하고 서버는 `java -jar` 실행만 한다. 판단 기준은 "이 컴퓨터가 하는 일이 빌드인가 실행인가". 진단 도구(`jstack`·`jcmd`)가 필요해지면 그때 JDK 를 얹는다 (YAGNI)
  - `-headless` 는 화면 출력용 라이브러리(X11·사운드)를 뺀다. 모니터 없는 서버엔 쓸 일이 없고 `ImageIO` 는 그대로 된다 (Sprint 2 영수증 처리 영향 없음)
  - 주의 — `java` 미설치 상태에서 나오는 "can be installed with" 목록은 command-not-found 의 별도 스냅샷이라 버전이 뒤처진다. 실제 설치 값은 `apt-cache policy` 의 Candidate
  - 실행 유저 — `sudo useradd --system --shell /usr/sbin/nologin --no-create-home ledger`
    - 실측 `ledger:x:999:987::/home/ledger:/usr/sbin/nologin` — UID 999(<1000) 시스템 계정 / 셸 `nologin` / `/home/ledger` 는 **경로만 적힌 것이고 디렉토리는 없다**(`mysql` 계정의 `/nonexistent` 와 같은 성격)
    - **계정은 사람만 쓰는 게 아니다** — 이미 이 서버의 MySQL 이 `mysql` 계정으로 돌고 있다. 아무도 로그인한 적 없지만 잘 돈다. 목적은 앱이 뚫렸을 때 공격자가 얻는 것을 "서버 전체"가 아니라 "`ledger` 가 만질 수 있는 것"으로 좁히는 것
    - **비번을 만들지 않는다** — `/etc/shadow` 두 번째 칸이 `!`(잠김). systemd 는 root 로 돌다가 `User=ledger` 로 **내려가는** 것이라 인증 절차 자체가 없다. 비번을 만들면 막아지는 경로 없이 관리할 비밀만 하나 는다 — ADR-043 에서 MySQL root 에 내린 판단과 같은 논리
    - 셸이 없어도 `sudo -u ledger <명령>` 은 된다. 막히는 건 대화형 로그인(`su - ledger`)뿐이라 디버깅에 지장 없음
    - `/etc/passwd` 의 `x` 는 "비번 없음"이 아니라 "해시는 `/etc/shadow` 에 있다"는 표시. `passwd` 는 누구나 읽을 수 있어서 해시를 root 전용 파일로 옮긴 결과
  - 이 단계가 못 막는 것 — 앱이 뚫리면 `ledger_db` 안은 그대로 털린다 (ADR-043 "감수"). 전용 유저는 DB **밖의** 서버를 지키는 장치다
  - 설치 후 `free -h` available **915Mi** (2-2 직후 879Mi). JRE 는 깔아둔 것만으론 메모리를 쓰지 않는다 — 실제 판단은 2-5 에서 앱을 올린 뒤
- **2-4 systemd — ✅ 2026-09-18** — `ExecStart` 는 `java -jar`, `Restart=always`(운영 3종의 "자동 재시작"이 여기서 같이 해결), `User=ledger`.
  환경변수는 `EnvironmentFile=`(권한 600)에 — `SPRING_PROFILES_ACTIVE=prod` / `DB_USERNAME` / `DB_PASSWORD` / `JWT_SECRET`
  - **자리 결정** — jar 는 `/opt/ledger/`, 설정은 `/etc/ledger/ledger.env`
    - `/opt` — 패키지 매니저 밖에서 온 자체 완결 소프트웨어의 자리(FHS). apt 로 깐 MySQL·Java 는 `/usr` 밑, 직접 올리는 jar 는 `/opt`
    - `/etc` — 설정의 자리(nginx → `/etc/nginx`, MySQL → `/etc/mysql`). **나누는 이유는 수명이 다르기 때문** — jar 는 배포마다 갈아끼우고 비밀값은 서버가 사는 동안 그대로. 같은 폴더에 두면 3단계 CD 가 `/opt/ledger` 를 비우다 비밀값을 같이 지우는 사고가 난다
    - `/etc/default/ledger` 가 아닌 이유 — 그 자리는 배포판 패키지 관례. 직접 올린 앱은 자기 이름의 디렉토리를 갖는다
    - 실무 규모에선 파일 대신 Secrets Manager·Parameter Store 에서 기동 시 받아온다. 한 대짜리엔 과함 — "지금은 파일 + 600, 서버가 늘면 Parameter Store"
  - **소유권** — `ledger.env` 는 `root:root 600`. systemd(PID 1, root)가 유닛을 준비하며 파일을 읽고 값을 환경변수로 넘긴 **뒤에** `User=ledger` 로 내려가므로 앱 계정은 이 파일을 읽을 필요가 없다. `/opt/ledger` 는 올리는 쪽(지금은 `ubuntu`, 3단계부터 배포 계정) 소유 755, jar 644 — **앱은 자기 실행 파일을 고칠 수 없어야 한다**(뚫린 앱이 jar 를 바꿔치기하는 경로 차단)
  - `JWT_SECRET` 형식 — 코드가 `Keys.hmacShaKeyFor(secret.getBytes())` 로 문자열 바이트를 그대로 키로 쓴다(2026-09-18 grep). 32바이트 이상이면 HS256, 64바이트 이상이면 HS512. `openssl rand -base64 48`(64글자) 로 만들면 된다. base64 는 `#`·공백이 없어 EnvironmentFile 에 따옴표 없이 넣어도 안전
  - **실측 (2026-09-18)** — `/etc/ledger` `drwxr-xr-x root root` / `/opt/ledger` `drwxr-xr-x ubuntu ubuntu` / `ledger.env` `-rw------- root root`.
    값 4개는 `sudo awk -F= '{print $1, length($2)}'` 로 **이름과 길이만** 확인(값은 화면·채팅에 띄우지 않는다) — `SPRING_PROFILES_ACTIVE 4` / `DB_USERNAME 10` / `DB_PASSWORD 32` / `JWT_SECRET 64`
    JWT 시크릿은 `openssl rand -base64 48`. `ledger.env` 는 `=` 양옆 공백·따옴표 없이, 값에 `#` 가 있으면 systemd 가 주석으로 보므로 그 줄만 큰따옴표
  - **유닛 파일** `/etc/systemd/system/ledger.service` — `Type=simple` / `User`·`Group=ledger` / `WorkingDirectory=/opt/ledger` / `EnvironmentFile=/etc/ledger/ledger.env` / `ExecStart=/usr/bin/java -jar /opt/ledger/ledger.jar` / `Restart=always` + `RestartSec=5` / `SyslogIdentifier=ledger` / `WantedBy=multi-user.target`
    - `After=network-online.target mysql.service` 만 쓰고 **`Requires=` 는 안 쓴다** — `Requires` 는 "MySQL 이 죽으면 나도 죽어라" 까지라 MySQL 재시작마다 앱이 같이 내려간다. 순서만 정하고 접속 실패는 `Restart=always` 가 재시도로 흡수
    - `RestartSec=5` 가 없으면 기동 실패 시 초당 수십 번 재시도하며 로그를 태운다
    - `ExecStart` 는 **절대경로 필수** — systemd 는 셸이 아니라 `PATH` 를 보지 않는다. `/usr/bin/java` 는 `/etc/alternatives/java` 심볼릭 링크(데비안 alternatives)지만 systemd 가 따라간다. 나중에 다른 JDK 를 깔면 이 스위치가 옮겨갈 수 있음
    - **jar 이름을 `ledger.jar` 로 고정** — 빌드 산출물은 `ledger-0.0.1-SNAPSHOT.jar`. 버전이 파일명에 박히면 버전 올릴 때마다 유닛 파일을 고쳐야 한다. 올릴 때 이름을 바꿔 두면 유닛 파일은 영원히 그대로 (ETC)
    - 보안 4줄 — `NoNewPrivileges`(도중 권한 상승 차단) / `PrivateTmp`(전용 `/tmp`) / `ProtectSystem=full`(`/usr`·`/boot`·`/etc` 읽기 전용) / `ProtectHome`(`/home` 비노출). 앱은 자기 디렉토리 읽기 + MySQL 접속이 전부라 넷 다 걸어도 지장 없다. 실무 유닛 파일의 기본 세트
    - `EnvironmentFile` 은 `ProtectSystem` 과 무관 — systemd(root)가 샌드박스를 걸기 **전에** 읽어 환경변수로 넘긴다
  - **등록** — `systemd-analyze verify`(우리 파일 지적 0줄 = 통과. `xfs_scrub_all` 경고는 우분투 기본 유닛이라 무시) → `daemon-reload`(디스크를 다시 읽어라. 유닛 파일 만들거나 고칠 때마다) → `enable`(부팅 시 자동 기동 등록 — `multi-user.target.wants/` 에 심볼릭 링크) → `is-enabled` 가 `enabled`
    - **`enable` ≠ `start`** — `enable` 은 "앞으로 부팅할 때마다", `start` 는 "지금". 별개라서 하나만 하면 반쪽이 된다. 환경변수는 `start` 하는 순간 읽힌다
    - 여기서 **운영 3종의 "자동 재시작"이 닫혔다** — 앱이 죽으면 `Restart=always`, 서버가 재부팅되면 `enable`
  - 남은 것 — jar 가 없어 `start` 는 2-5 에서
  - ADR-045 후보 — "설정은 `/etc`, 실행 파일은 `/opt`, 비밀값 파일은 root 소유" (문제 → 대안 → 근거 → 감수 로 정리)
- **환경별로 넣어줄 값** — 코드가 읽는 환경변수는 `DB_USERNAME` / `DB_PASSWORD` / `JWT_SECRET` 3개가 전부 (2026-09-17 grep). 앱이 뜨는 컴퓨터 3대에 각각 넣는다

| | 맥북 | GitHub Actions | EC2 |
|---|---|---|---|
| 위 3개 | 셸 환경변수 (기존) | `ci.yml` 일회용 값 (기존) | `EnvironmentFile` — **새로 만든 값** |
| `SPRING_PROFILES_ACTIVE` | `local` (STS Profile 칸) | 없음 | `prod` |
| `QA_PASSWORD` | 셸 환경변수 | 4단계에서 CI 전용 값 | 없음 (운영에선 QA 안 돌림) |

  - **세 환경의 값은 서로 달라야 한다** — `ci.yml` 의 `JWT_SECRET` 은 공개 저장소에 있다. EC2 가 같은 값을 쓰면 누구나 로그인 토큰을 위조할 수 있다. EC2 용은 새로 생성한 긴 랜덤 값
  - 나중에 늘어날 값 — 도메인·S3 버킷(5단계) / SES 발신 주소(비번 재설정) / Clova 키(Sprint 2) / 카카오 키(Sprint 5·소셜 로그인). 그 기능을 만들 때 이 표에 한 줄씩
- 첫 배포는 손으로 — 로컬에서 `./gradlew build` → `scp` → `systemctl start`. 자동화(3단계) 전에 수동으로 한 번 성공시켜야 무엇을 자동화하는지 안다
- 검증 — `journalctl -u ledger` 에 `profile is active: "prod"` + SQL 로그 없음 / 가입·로그인·거래 등록 / 오전 9시 이전 "오늘" 거래가 400 안 나는가 / 인스턴스 재부팅 후 자동 기동

### 3단계 가이드 — CD 최소판 (게이트 없이)

- `ci.yml` 에 `deploy` 잡 추가. `needs: build` + **push to main 일 때만**(PR 에선 배포 금지) 조건
- build 잡이 만든 jar 를 artifact 로 넘겨 deploy 잡이 받는다 — **테스트한 그 jar 를 배포** (deploy 잡에서 다시 빌드하지 않는다)
- 주의 — `build/libs` 에 `-plain.jar` 도 같이 생긴다. 실행 jar 만 집거나 `build.gradle` 에서 plain jar 를 끈다
- GitHub Secrets — `EC2_HOST` / `EC2_USER` / `EC2_SSH_KEY`. **배포 전용 키쌍**을 새로 만든다 (내 접속용 키와 분리)
- EC2 쪽 — 배포 계정이 `systemctl restart ledger` **만** 비밀번호 없이 sudo 가능하게 (sudoers 한 줄)
- 흐름 — jar 전송 → restart → 헬스 확인(curl 로 200 이 올 때까지 대기, 안 오면 잡 실패)
- **여기서 결정할 것 — 헬스 확인 경로.** actuator 는 의존성에 있고 기본 노출은 `health` 하나뿐인데, `SecurityConfig` 의 `anyRequest().authenticated()` 가 `/actuator/**` 도 덮어서 토큰 없이는 **401** (2026-09-17 확인).
  대안: `/actuator/health` 만 permitAll / EC2 안에서 `localhost:8080` 으로 확인(밖엔 안 열림) / 정적 페이지 200 으로 대체. UptimeRobot(5단계)도 같은 경로를 쓰게 되므로 같이 정한다
  - 절대 하지 않을 것 — `management.endpoints.web.exposure.include` 에 `*` 나 `env`. `/actuator/env` 는 환경변수(DB 비번·JWT 시크릿)를 웹으로 보여준다
- 검증 — 화면 문구 하나 바꿔 push → Actions 초록불 → 배포 URL 에 반영

### 5단계 메모 — 도메인은 HTTPS 직전에 산다

- IP 주소만으로는 뒤 기능이 막힌다 — HTTPS 인증서(사실상 도메인 필요) / PWA 설치(HTTPS 필수) / 카카오 로그인·지도 콘솔 등록 / Refresh 쿠키 `Secure`(ADR-010)
- 구조 — 사용자 → Nginx(80 은 443 으로 돌려보냄, 443 에서 인증서 처리) → `localhost:8080`. 보안 그룹에 80·443 만 열어둔 이유 (aws-인프라.md)
- 도메인이 생기면 `application-prod.yml` 을 처음 만들 시점일 수 있다 (쿠키 `Secure`, CORS 등 운영 전용 값 — ADR-042)

### 4단계 가이드 — 게이트 (qa 146케이스)

- 구조 — `build` → `qa`(게이트) → `deploy`. deploy 의 `needs:` 에 qa 를 넣으면 실패 시 배포가 막힌다.
  QA 는 **GitHub Actions 러너(일회용) 안에서** 돈다. EC2·운영 DB 는 건드리지 않는다 (`reset-data.sql` 은 데이터를 지우므로 운영에서 돌리면 안 됨)
- `_lib.sh` 고칠 것 (위임, 기본값은 지금 로컬 동작 그대로) — 2026-09-17 실측
  - `ask_reset` 의 `read ANS` — 환경변수(예: `QA_RESET=y`)가 있으면 묻지 않고 진행
  - `mysql -u root -p` — 호스트·계정·비번을 환경변수로. 러너의 MySQL 은 서비스 컨테이너라 소켓이 아닌 `-h 127.0.0.1`
  - **`summary()` 가 FAIL 이 있어도 종료코드 0** — `FAIL > 0` 이면 `exit 1`. 이게 없으면 게이트가 항상 통과한다
  - `BASE` 도 환경변수로 덮을 수 있게
- 워크플로 `qa` 잡 — MySQL 서비스 컨테이너(지금 build 잡에 있는 것과 같은 설정) → jar 백그라운드 기동 → 헬스 대기 루프 →
  계정 시드(가입 API curl 2회: `test@test.com`, `rollback@test.com`, 비번은 CI 전용 `QA_PASSWORD`) → `qa/test-*.sh` 4개
- 검증 — 일부러 서비스 검증 한 줄을 깨서 push → qa 빨간불 + **deploy 가 안 도는 것** 확인 → 되돌림
- 끝나면 — CLAUDE.md 협업 방식·qa-체크리스트에 "push 하면 CI 가 146케이스를 돌린다" 반영

---

## Sprint 1 — 남은 순서

~~거래 CRUD(8단계 화면)~~ ✅ → ~~통계 화면(위임)~~ ✅ (2026-09-17) → ~~거래 화면 합산 교체~~ ✅ (2026-09-17, ADR-040) → **1차 배포(CD·EC2·HTTPS, SES 샌드박스 해제 신청)** → 로그인 잠금 → 비번 재설정(메일) → Sprint 2

배포를 앞당긴 이유는 기획서 스프린트 표 아래 참고.

---

## Sprint 1 종료 시 반드시 다시 꺼낼 것

- [ ] 리팩토링 판단 기준 — "다음 기능이 이 코드를 복제하게 되는가?" 그렇다면 그 기능 **직전**에, 아니면 여기 적고 넘어감 (ADR-038 정리 때 세운 기준)
- [ ] 카테고리 이름 중복 차단 (ADR-018). **입구 정규화(앞뒤 공백 제거)는 2026-09-17 선반영** — 남은 건 중복 판정 자체 (ADR-044)
- [ ] `USER_ID` 공개 핸들 — 유저 검색 기능 때 가입 폼에 추가 (ADR-013)
- [ ] 운영 프로파일 분리 — 배포 직전 → **진행 중** (위 "1차 배포" 1~2단계, ADR-042)
- [ ] 휴면 전환 스케줄러 — Sprint 2 스케줄러 때 (ADR-029)
- [x] `@Max` 메시지 문구 "1조원 미만" → "이하" (이미 반영돼 있었음, 2026-09-16 확인)
- [ ] 소셜 로그인 — 배포 후, 카카오 1사부터 (개발자 콘솔에 배포 URL 등록 필요). 기획서 스프린트 표·ADR은 Sprint 1 전체 리뷰 때 정리
- [ ] 운영 3종 — systemd 자동 재시작 / DB 백업 크론 → S3 / UptimeRobot. 이중화 대신 이걸로 (ADR-002 보완, 배포 직전)
- [ ] **배포 시 타임존 3계층 확인** — OS·MySQL·JVM 모두 KST 인지 (ADR-026 주의 참고). 로컬에선 안 드러남. **OS·MySQL 은 2-2 에서 확인 완료(2026-09-17), JVM 은 2-5 에서** (오전 9시 이전 '오늘' 거래가 400 안 나는가)
- [ ] **뷰 대공사** — 메시지 표시를 한 곳으로 모은다. 지금은 세 갈래: `handleConflict()` alert /
      `removeTransaction()` alert / submit 의 `resultMessage`(모달 안). 투박해도 깔끔·단순하게,
      모달 결과 메시지 UI 를 만들고 셋을 함수 하나로 합칠 것. 달력 뷰·PWA 와 같이 묶어도 됨
      + 수정 모달의 지출/수입 탭이 "종류 선택"임을 드러내기 (필터처럼 보임. 동작은 의도된 것 — 타입은 카테고리의 그림자, ADR-008)
      + 조회 실패(500·네트워크) 시 화면이 조용히 멈춤 — `loadTransactions`·`loadStatistics` 에 catch 없음. 메시지 UI 만들 때 같이
      + 통계 봉투를 펼치면 대분류 직접 거래 줄이 대분류와 같은 이름으로 보임 — 헷갈리면 "(직접)" 표시 (2026-09-17 화면 QA)
- [ ] 달력 뷰 — 목록/달력 토글. 백엔드 변경 없음(같은 월 조회 API를 다르게 그리는 것)이라 급하지 않음. 개인 통계 끝나고 판단
- [ ] 패스키(WebAuthn) — 지문·FaceID로 비밀번호 대체. 웹·PWA 공통 표준. Sprint 5 이후
- [x] **거래 화면 합산 → 서버 값으로 교체 (ADR-038 미룬 것)** — 2026-09-17 완료. `/api/statistics` 값 표시, 세 요청 `Promise.all` (ADR-040).
      같은 날 서버/뷰 전체 스캔 — 서버 위반 0, 프론트 위반은 이 1건. 회색 판정은 ADR-040. signup 입력 보조(`maxlength`) 추가
      (이전 기록) `transaction.js render()` 가 수입/지출/잔액을 직접 합산 중.
      통계 API 가 `totalIncome / totalExpense / balance` 를 내려주게 되면 그 값을 읽어 표시만 하도록 교체.
      타이밍: 통계 조립(최상위 DTO) 끝난 직후. 그 전엔 건드리지 않음 (통계 API 없이 바꾸면 요청 하나가 더 생김)
- [ ] 거래 목록 "N건" → 페이징 붙일 때 서버 totalCount 로 (지금은 보이는 줄 수라 뷰에 둠, ADR-040)
- [ ] **바꾸기 쉬운 구조 — 시점별 (ADR-041, 2026-09-17 스캔).** 지금 고치지 않는다. 해당 기능 첫 단계에서 먼저 꺼낼 것
      + **배포 직전** — 운영 프로파일: `application.yml` 의 `localhost` DB URL · 매퍼 `DEBUG` 로그 (기존 "운영 프로파일 분리"와 같은 작업)
      + ~~**CD 만들 때** — `qa/test-*.sh`(146케이스)를 배포 전 게이트로 묶을지 결정. CI 는 `@Test` 1개뿐이라 회귀가 수동~~ → **도입 결정** (2026-09-17, ADR-042). 구현은 "1차 배포" 4단계
      + **로그인 잠금 직전** — 예외를 의미별 부모 클래스로 묶어 핸들러 수 고정(현재 15개) / `/api/users/me` 응답을 `LoginResponseDto` 에서 분리
      + **Sprint 2, 거래에 필드가 붙기 직전** — `PersonalTransactionService` 값 채우기·응답 변환 한 곳으로(create/update 2벌), 소유권 확인 private 메서드(7곳)
      + **통계 다음에 건드릴 때** — 비율 계산 3벌 → `percent(part, whole)`, 추이 개월 수 `5`/`6` 리터럴 한 상수로
      + **달력 뷰 직전** — 월 이동(year/month/changeMonth) util 추출. `category.js` 의 `editingId`(hidden input)도 `transaction.js` 처럼 JS 변수로
      + **Sprint 1 전체 리뷰** — `PersonalTransactionService` 힌트 블록 주석 100줄 → "왜"만 남기기 (체크리스트 8번)
- [ ] **Sprint 2, Ollama 설치 직전** — 인스턴스 t4g.small → t4g.medium (중지 → 유형 변경 → 시작, ADR-002 보완). 그 전에도 `free -h` 로 메모리가 빠듯하면 올린다
- [ ] **세 환경 MySQL 버전 정렬 — 4단계 게이트 때** (2026-09-17 실측: 맥 **26.7** / CI **8.0** / EC2 **8.4.11**). 셋이 전부 다르다
      + 결정: CI 이미지를 `mysql:8.4` 로 올린다 — 게이트는 **운영과 같은 버전**에서 통과해야 의미가 있다. 8.0 은 2026-04 지원 종료라 어차피 못 쓴다
      + 맥 26.7 은 그대로 둔다 (개발 편의). 맞춘 건 collation 이고 버전까지 같게 하지는 않는다 — 다르면 드러나는 곳은 게이트이므로 CI 만 맞추면 된다
      + Flyway 가 `Using MySQL 26.7 which is newer than the version Flyway has been verified with` 경고를 내는 것도 이 때문. 로컬 전용 경고라 무시
- [ ] `qa/reset-all.sh` 의 `signup()` 이 `-o /dev/null` 로 **응답 본문을 버려서 실패 원인이 안 보인다** (2026-09-17, `QA_PASSWORD` 가 비번 규칙에 안 맞아 400 이 났을 때 원인 추적에 시간을 씀). 실패했을 때만 본문을 찍도록 — 4단계 `_lib.sh` 비대화 모드 작업에 같이 (위임)
- [ ] **1차 배포 완료 직후 ① — 기획서·노션 재점검** (2026-09-18 결정). 배포하며 어긋난 걸 한 번에 맞춘다 — t3.medium → t4g.small(예산 5.7만 → 3.3만) / Redis "컨테이너" → 지금은 앱 메모리·MySQL, 서버 2대부터 Redis / Sprint 1 의 소셜 로그인·비번 재설정 뒤로 / Sprint 5 의 배포 앞당김 / EBS 20 → 30GB. 저장소 `기획서.md` 와 노션 원본 둘 다
- [ ] **1차 배포 완료 직후 ② — 배포·인프라 총정리 문서** (2026-09-18 요청). "어떤 식으로 이루어졌는가"를 한 장에 — 요청 흐름(사용자 → Nginx → `localhost:8080` 앱 → `localhost:3306` MySQL) / 계정 표(`ubuntu` 사람 · `ledger` 앱 · `ledger_app` DB · CD 배포 계정) / 디렉토리·소유권 표(`/opt/ledger` · `/etc/ledger/ledger.env` · 유닛 파일) / 환경변수 표(세 환경) / 배포 절차(수동 → CD) / 단계별 실측 이력. `aws-인프라.md` 를 확장할지 `docs/배포.md` 를 새로 둘지는 그때 결정
- [ ] **Redis 도입 직전 — 정말 필요한가부터** (2026-09-18). 서버 한 대면 카운터를 공유할 상대가 없고 2GB 에 상주 프로세스 하나가 더 는다. 호출 제한 카운터는 앱 메모리, Refresh Token 은 MySQL 테이블(기획서의 정리 스케줄러 방식). 넣기로 하면 apt 패키지 vs 도커 결정 — 앱은 systemd 라 도커를 깔면 "왜 앱은 아니냐"가 생긴다
- [ ] **Sprint 2, 이미지가 서버를 거치게 되면** — 앱이 쓰는 파일 자리 `/var/lib/ledger`(`ledger` 소유)를 따로. `/opt/ledger` 는 앱이 못 쓰는 자리로 유지
- [ ] 서비스 변수명 통일 — `newPersonalTransaction` / `transaction` 혼재 여부 확인
- [ ] 패키지 도메인별 분리 — Sprint 4 직전 (ADR-037)
- [ ] 들여쓰기 통일 — 자바 파일 탭 29 / 스페이스 16 혼재. Sprint 1 전체 리뷰 때 포매터(스페이스 4 or 탭) 하나로 정하고 한 커밋에 일괄. 기능 커밋에 섞지 않는다
      + static 도 — 탭 들여쓰기: `user/login·signup` js·html 4파일 / 파일 끝 개행 없음: `error/404·500`, `main.js`, `authGuard.js`, `buttonUtil.js`, `user/` 4파일 (2026-09-17 실측)
- [x] **`month` 범위 검증 (500 → 400)** — A안 채택 (2026-09-16). `@RequestParam @Min(1) @Max(12) int month` + `HandlerMethodValidationException` 400 핸들러.
      `/api/statistics`, `/api/transactions` 둘 다 적용. B안(`DateTimeException` 핸들러)은 서버 내부 날짜 버그까지 "사용자 잘못"으로 덮어서 탈락 (ADR-039)
- [ ] **멱등성(중복 요청 서버 방어) — Sprint 4 "깊게 팔 영역" 후보** (2026-09-17 결정: 로드맵에 적는다)
      + 지금 — 화면에서만 막는다. `withButtonLock`(`buttonUtil.js`)이 submit 버튼을 잠금: 거래·카테고리·가입·로그인 4곳
      + 뚫리는 경로 — curl·스크립트 직접 호출 / 응답을 못 받은 클라이언트의 재시도(모바일 네트워크, PWA) / 더블 탭이 잠금보다 빠른 경우. 결과는 **같은 거래가 두 줄** = 돈 기록이 틀어짐 (낙관적 락은 "수정 충돌"만 잡고 "생성 중복"은 못 잡는다, ADR-006)
      + 왜 Sprint 4 — 공동 가계부에선 중복 지출이 곧 정산 금액 오류라 피해가 남에게 간다. 그 전엔 본인 화면에서 보이고 지우면 됨
      + 그때 정할 것 — 멱등 키(클라이언트가 요청마다 UUID, 헤더 `Idempotency-Key`) 저장 위치(DB 유니크 vs Redis TTL) / 같은 키 재요청 시 응답(첫 응답 재생 vs 409) / 어느 API 까지(POST 생성만?)
      + qa-체크리스트 "같은 요청을 두 번 보내면?" 축의 서버 쪽 답이 이것
- [ ] curl QA → JUnit 단위 테스트 전환 — Sprint 4 "깊게 팔 영역" (docs/qa-체크리스트.md 한계 참고)

---

## 현재 파일 구조 (static)

```
views/
  main.html
  user/         login.html, signup.html
  personal/     personal-main.html, category.html, statistics.html
js/
  main.js
  util/         apiClient.js, authGuard.js, buttonUtil.js, format.js, theme.js
  user/
  personal/     category.js, transaction.js, statistics.js
css/
  common.css
error/
  404.html, 500.html
```

하위 네비(`.sub-nav`)로 가계부 ↔ 카테고리 ↔ 통계 이동.
