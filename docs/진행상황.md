# 진행 상황

## 배경

- Ledger V1을 1인 프로젝트로 완주. V2는 AI OCR·로컬 LLM 분류·지도 클러스터링·1/N 정산 포함
- V3.0은 24시간 상시 운영 실서비스로 배포하고 홍보까지 할 계획
- 2026년 9월 내 Sprint 5까지 목표 (하루 12시간 투입 가능). 현실적 추정은 27~36일 소요라 9월엔 Sprint 2 완료 + 배포 URL이 목표
- 스프린트마다 "깊게 팔 영역" — Sprint 1 트랜잭션·검증 설계 / Sprint 2 OCR 파이프라인 분기 / Sprint 3 임베딩·코사인 유사도 / Sprint 4 그리디 정산·낙관적 락(JUnit까지, 멱등성 후보)

---

## Sprint 1 — 완료 항목 (2026-09-11 기준)

### 회원 · 인증
- 회원가입/로그인 + JWT 인증 — `User`, DTO들, `UserMapper` + XML, `UserService`, `UserController`(`/api/users/*`), BCrypt, Validation, `JwtTokenProvider`, `JwtAuthenticationFilter`, `SecurityConfig`
- 로그인 시 탈퇴(W) 차단 + `user_last_login_at` 갱신
- 인증 응답 정리 — `SecurityConfig` STATELESS + 401 entryPoint. `apiClient.js` 토큰 자동 첨부·401 리다이렉트·204 처리
- 미분류 자동생성 — `signup()`에 `@Transactional` + `PersonalCategoryMapper` 직접 주입. E/I 2개 생성(parent null, emoji null). `PersonalCategory.DEFAULT_CATEGORY_NAME` 상수. 롤백 테스트 확인

### 개인 카테고리 CRUD
- `PersonalCategory`, `CategoryRequest/ResponseDto`, Mapper + XML, Service, Controller(`/api/categories`). 소유권 검증, 소프트 딜리트
- 예약어 차단 — `ReservedCategoryNameException`(400)으로 '미분류' 이름 생성/수정 차단(trim 포함). 미분류 수정·삭제 차단(403)
- `validateParent` — 부모 존재/소유/타입일치/2계층/미분류아님/자기자신아님 + update 시 자식 있으면 소분류로 못 내려감. `InvalidCategoryHierarchyException`(400). 12케이스 QA 통과
- 하위 카테고리 있는 대분류 삭제 거부 (`countChildren`)
- 카테고리 관리 화면 — `views/personal/category.html`, `js/personal/category.js`. 목록/탭(E,I)/모달 기반 CRUD, 2계층 들여쓰기, 미분류 버튼 숨김, 상위 select 제약(자기자신·미분류·타입·자식유무 disabled). **QA 22케이스 통과**

### 공통
- 에러 응답 분리 — API 텍스트 / 페이지 HTML (ADR-012). JSON 파싱 실패·타입 불일치 400 핸들러. 최종 Exception 핸들러는 고정 문구
- `RootController` — `/` → `/views/main.html` 리다이렉트
- 다크모드 — `js/util/theme.js`. OS 설정 기본값 + 토글, localStorage 저장. `common.css` 색상 변수화
- GitHub Actions CI (빌드 + 테스트) 구축. CD는 아직 없음
- `qa/test-category-hierarchy.sh` — 카테고리 12케이스 회귀 스크립트. JUnit 전환 전까지 유지

---

## Sprint 1 — 진행 중: 개인 거래 CRUD

설계 결정은 ADR-006, 008, 009, 023~028 참조.

| 단계 | 상태 |
|---|---|
| 1. `PersonalTransactionRequestDto` / `ResponseDto` | ✅ |
| 2. `PersonalTransaction` 도메인 | ✅ |
| 3. `PersonalTransactionMapper` 인터페이스 + XML (7개 statement) | ✅ |
| 4. `PersonalTransactionService` — 소유권 검증, transType 파생, 낙관적 락 판정 | ✅ |
| 5. Controller(`/api/transactions`) + 예외 3개 409/404/403 등록 | ✅ |
| 6. 카테고리 삭제 시 미분류 이관 (`deleteCategory`에 `@Transactional` + 일괄 UPDATE) | ✅ |
| 7. JVM 기본 타임존 Asia/Seoul 고정 (`@PostConstruct`) | ✅ |
| 8. 거래 관리 화면 (`views/personal/personal-main.html`, `js/personal/transaction.js`) — 위임. 화면 QA 에서 삭제된 참조 문제 발견·수정 (ADR-006 보완) | ✅ |
| 9. API QA — `qa/test-transaction.sh` **52케이스 전체 통과** (2026-09-12) | ✅ |

### QA 결과 요약 (52/52)

- 정상 11 / 실수 19 / 악의적 13 + 응답 body 검사 9
- 검증된 것: `transType` 서버 파생, `version` 0→1→2 연속 수정, 12월 경계,
  조인 값(`categoryName`·`categoryEmoji`), 소유권 403, 낙관적 락 409,
  Mass Assignment 방어(body 에 `userNum`/`transType`/`version` 끼워넣어도 무시)
- 남은 것: 없음
- 7단계 후 회귀 재실행 — `test-transaction.sh` 52 + `test-category-transfer.sh` 35, 전부 통과 (2026-09-13)

### 6단계 이관 QA 결과 (35/35, 2026-09-13)

`qa/test-category-transfer.sh` — 상태코드가 아니라 **부수효과**(거래가 옮겨졌는가)를 검사.
삭제 후 목록을 재조회해 `categoryNum`·`categoryName`·`version`을 확인한다.

- 경계값 — 거래 0건(이관 0행도 정상) / 1건 / 3건 전부 이동
- 타입 — 지출→지출 미분류, 수입→수입 미분류. E·I 공존 시 반대 타입 거래 불변
- 순서 의존 — 이관이 올린 `version` 때문에 옛 version 수정은 409, 새 version은 200.
  이미 삭제된(`use_yn='N'`) 거래는 이관 대상 아님 (DB 직접 확인)
- 부수효과 — 남의 카테고리 403 / 자식 있는 대분류 400 일 때 **이관도 일어나지 않음**
  (검증이 이관보다 앞에 있다는 증명). 미분류 자체 삭제 403 — 이관 목적지 보호
- `defaultCategory` null 체크는 넣지 않음 — 미분류는 `signup()`이 항상 생성(ADR-020)하고,
  깨지는 경로는 SQL 로 유저를 직접 INSERT 한 경우뿐. NPE 가 나도 `@Transactional` 이 롤백

카카오맵 장소 입력은 Sprint 5. 지금은 위치 없이.

---

## Sprint 1 — 완료: 개인 통계 (2026-09-16)

설계 결정은 ADR-038, 039 참조. API 는 하나 — `GET /api/statistics?year=&month=`

| 단계 | 상태 |
|---|---|
| 1. 카테고리별 집계 (봉투 구조) — `findCategoryStats` → `CategoryStatDto`(대분류) ⊃ `SubCategoryStatDto`. 비율은 부모 대비 | ✅ |
| 2. 6개월 추이 — `findMonthlyStats` → `MonthlyStatDto`. 보는 달 포함 롤링 6개, 빈 달 0 채움, balance 서버 계산 | ✅ |
| 3. 조립 — `StatisticsResponseDto`(totalIncome/totalExpense/balance/expenseRatio + expenseList/incomeList/monthlyList), `getStatistics()` | ✅ |
| 4. month 1~12 입구 검증 400 (A안) | ✅ |
| 5. 임시 엔드포인트 `/expense` `/income` `/monthly` 삭제. `test-statistics-monthly.sh` 는 `test-statistics.sh` 에 흡수 | ✅ |
| 6. QA — `qa/test-statistics.sh` **38/38** | ✅ |
| 7. 화면 (`views/personal/statistics.html`, `js/personal/statistics.js`) — 위임. 브라우저 QA 통과 (2026-09-17) | ✅ |

### QA 요약 (38/38)

- 요약 4개 = 봉투 합 = 추이 마지막 원소 (세 출처가 같은 값인지 — 1-a/1-b/1-e)
- 수입 대비 지출 — 수입 0 이면 `null`(계산 불가, 프론트 `-` 표시), 0.4 / 200.0 / 66.7(HALF_UP)
- 봉투 — 대분류 직접 거래 + 소분류가 한 봉투. 자식 비율은 부모 대비 (75.0 + 25.0)
- 빈 목록은 `[]`, 타인 거래 미포함, 삭제 거래 제외, 연도 넘김
- month 13/0/-1 → 400 "허용 범위", 1·12 → 200. 누락·형식 400, 무토큰 401

### 로드맵에 남긴 것

- (정리 커밋에서 해소) 카테고리 집계 조회 1회로 — `buildCategoryStats(rows, type)` 순수 함수화, `getExpenseStats/getIncomeStats` 삭제

---

## Sprint 1 — 진행 중: 1차 배포

설계 결정은 ADR-042 · 046 · 047 · 048 참조. 순서는 **배포할 물건(yml) → 서버 → 자동 배포 → 검문소 → 운영 보강**.
3과 4를 나눈 이유 — 같이 만들면 실패했을 때 배포 문제(SSH·systemd)인지 테스트 문제인지 구분이 안 된다.

| 단계 | 상태 |
|---|---|
| 1. 운영 프로파일 — yml 분리 (직접). 기동 확인: 프로파일 없음 → SQL 로그 없음 / `local` → SQL 로그 찍힘 (2026-09-17) | ✅ |
| 2. EC2 세팅 — 타임존·MySQL·Java·systemd. **프로파일 도입은 여기서 완성** (직접) | ✅ 2026-09-18 (2-1~2-5) |
| 3. CD 최소판 — jar 전송 + 재시작. 배포 1회 성공 확인 (직접) | ✅ 2026-09-20 (3-1~3-7, ADR-046·047·048, 트러블슈팅 13·14) |
| 4. 게이트 — `_lib.sh` 비대화 모드(위임) + 워크플로 QA 잡 + `needs:` (직접) | ✅ 2026-09-20 (4-1~4-5, CI #52~#55) |
| 5. 운영 보강 — HTTPS / DB 백업 크론 → S3 / UptimeRobot / 타임존 3계층 확인 / SES 샌드박스 해제 신청 | 🟡 5-1~5-4 ✅ 2026-09-21 (도메인·Nginx·HTTPS·actuator 차단·CSP, ADR-049) · 5-5 ✅ 2026-09-22 (DB 백업 → S3, ADR-050). 남은 것 — UptimeRobot / SES 신청 |

### 1단계 가이드 — 운영 프로파일 (yml)

- **규칙 하나** — `application-{이름}.yml` 은 그 프로파일이 켜졌을 때만 읽히고, 공통 `application.yml` **위에 덮어씌워진다(merge)**.
  그래서 프로파일 파일엔 **공통과 다른 값만** 적는다. 전체를 복사해 두면 mybatis 설정 하나 바꿀 때 두 파일을 고치게 된다 (ETC 위반)
- 분류 — "맥북과 EC2 에서 값이 달라야 하는가"

| 덩어리 | 달라야 하나 | 위치 |
|---|---|---|
| `spring.datasource` | 아니오 — 세 환경(맥북·CI·EC2) 모두 자기 컴퓨터의 MySQL(`localhost`), 계정·비번은 이미 `${ENV}` | 공통 |
| `mybatis` / `server.port` / `jwt` | 아니오 | 공통 |
| `logging.level.com.kyumin.ledger.mapper: DEBUG` | 예 — 운영에선 사용자 금액·메모가 SQL 바인딩 값으로 로그에 남는다 | `application-local.yml` |

- 할 일 — ① `application-local.yml` 엔 `logging:` 덩어리 3줄만 ② `application.yml` 에서 그 3줄 삭제 ③ STS Run Configuration → Profile 칸에 `local`
  - ③은 저장소가 아니라 STS 에 저장된다. 새 PC·STS 재설치 후 "SQL 로그가 안 보인다"면 Profile 칸부터 확인
- 검증 — `local` 켜고 기동: 로그에 `The following 1 profile is active: "local"` + 조회 시 SQL 찍힘.
  Profile 비우고 기동: `No active profile set` + SQL 안 찍힘 (= 운영의 모습)
- **기본값은 운영에 안전한 쪽** — DEBUG 를 공통에 두고 prod 에서 끄는 구조는 프로파일 지정을 깜빡하면 DEBUG 가 켜진 채 운영된다.
  공통엔 없고 local 에서만 켜면, 깜빡했을 때 "SQL 로그가 안 보임"으로 눈에 띄게·안전하게 실패한다
- `application-prod.yml` 은 **지금 안 만든다** — 넣을 값이 없다 (YAGNI). 파일이 없어도 `prod` 프로파일로 기동된다.
  운영에서만 다른 값이 처음 생길 때(Refresh 쿠키 `Secure`, S3 버킷, SES 발신 주소) 만든다
- DB URL 은 yml 에 그대로 — 세 환경에서 값이 같고 비밀이 아니다. `${DB_URL:...}` 는 DB 를 인스턴스 밖으로 옮길 때 그 한 줄만 바꾸면 된다 (ADR-042)
- CI 는 고칠 것 없음 (프로파일 없이 돈다)

### 2단계 가이드 — EC2 세팅

- **2-1 인스턴스 생성 — ✅ 2026-09-17** (ADR-002 보완). 값은 저장소에 적지 않는다 — IP·키 위치는 저장소 밖 개인 보관처에
  - `ledger-prod` / 서울 / Ubuntu 26.04 LTS arm64 / **t4g.small**(2GB — medium 으로 만들었다가 같은 날 내림) / 30GB gp3 / 크레딧 사양 Standard / 탄력적 IP 연결
  - 키 페어 `ledger-admin`(ED25519, `.pem`) — 내 접속용. CD 용 키는 3단계에서 따로
  - 보안그룹(`launch-wizard-1`) — 22(**내 IP 만**) / 80 / 443. **8080·3306 은 열지 않는다** (앱은 Nginx 뒤, DB 는 localhost 전용)
  - 주의 — 집 IP 가 바뀌면(공유기 재시작 등) SSH 가 타임아웃 난다. 보안그룹 인바운드 22번의 소스를 "내 IP"로 다시 지정
  - SSH 첫 접속 확인 ✅ / 예산 알림 `ledger-monthly` 월 $30 ✅ / 루트 MFA ✅ (폰 OTP, 내장 브라우저 로그인까지 확인). 만든 것 전체와 조작법은 [aws-인프라.md](aws-인프라.md)
  - 접속 배너의 시스템 시각이 **UTC** 였다 — 2-2 에서 OS 타임존부터 바꾸는 이유 (ADR-026)
- **2-2 타임존 → MySQL — ✅ 2026-09-17** (ADR-043 · ADR-044)
  - 순서가 중요 — **OS 타임존 먼저**(`timedatectl set-timezone Asia/Seoul`) → 그다음 MySQL 설치. MySQL 기본 `time_zone=SYSTEM` 이 OS 를 따라간다 (ADR-026 — JDBC `serverTimezone` 옵션으로 덮지 않는다)
  - 커널 업그레이드가 걸려 있으면(`Pending kernel upgrade`) **MySQL 설치 전에 재부팅**한다. 앱이 올라간 뒤엔 재부팅이 곧 서비스 중단이고, 옛 타임존으로 떠 있던 프로세스도 이때 정리된다
  - 실측 — OS `Asia/Seoul (KST, +0900)` · NTP active / MySQL **8.4.11**(`apt-cache policy` Candidate) · `active (running)` · `enabled`(재부팅 자동 기동) / `@@global.time_zone = SYSTEM` · `NOW()` 한국 시각
  - 보안 — `mysql_secure_installation` 실행. 다만 우분투 패키지는 **익명 사용자도 원격 root 도 처음부터 없다**(설치 직후 `mysql.user` 실측). 구멍을 막는 작업이라기보다 확인 작업이다. `root@localhost` 는 `auth_socket` 유지 (ADR-043)
  - `ledger_db` + 앱 전용 계정 `ledger_app`@`localhost` — 맥·CI 와 **같은 이름**, 비밀번호만 새로 만든 값. 테이블은 만들지 않는다 (첫 기동 때 Flyway `V1`)
  - 권한 확인 — `SHOW GRANTS` 가 `USAGE ON *.*` + `ALL PRIVILEGES ON ledger_db.*` 두 줄. `USAGE` 는 "접속만 가능" 이라는 뜻이라 정상
  - collation 은 **명시하지 않는다** — 기본값 `utf8mb4_0900_ai_ci` 가 그대로 세 환경 공통값이 된다 (ADR-044)
  - 메모리 — 설치 직후 `free -h` available 879Mi, 스왑 0. **2-5 에서 앱을 올린 뒤 available 이 300Mi 아래면 1GB 스왑 파일**을 만든다 (그때 판단)
- **2-3 Java 17 · 전용 실행 유저 — ✅ 2026-09-17**
  - `openjdk-17-jre-headless` **17.0.20** (arm64, universe). **JRE 로 충분** — 빌드는 맥·CI 가 하고 서버는 `java -jar` 실행만 한다. 판단 기준은 "이 컴퓨터가 하는 일이 빌드인가 실행인가". 진단 도구(`jstack`·`jcmd`)가 필요해지면 그때 JDK 를 얹는다 (YAGNI)
  - `-headless` 는 화면 출력용 라이브러리(X11·사운드)를 뺀다. 모니터 없는 서버엔 쓸 일이 없고 `ImageIO` 는 그대로 된다 (Sprint 2 영수증 처리 영향 없음)
  - 주의 — `java` 미설치 상태에서 나오는 "can be installed with" 목록은 command-not-found 의 별도 스냅샷이라 버전이 뒤처진다. 실제 설치 값은 `apt-cache policy` 의 Candidate
  - 실행 유저 — `sudo useradd --system --shell /usr/sbin/nologin --no-create-home ledger`
    - 실측 `ledger:x:999:987::/home/ledger:/usr/sbin/nologin` — UID 999(<1000) 시스템 계정 / 셸 `nologin` / `/home/ledger` 는 **경로만 적힌 것이고 디렉토리는 없다**(`mysql` 계정의 `/nonexistent` 와 같은 성격)
    - **계정은 사람만 쓰는 게 아니다** — 이미 이 서버의 MySQL 이 `mysql` 계정으로 돌고 있다. 아무도 로그인한 적 없지만 잘 돈다. 목적은 앱이 뚫렸을 때 공격자가 얻는 것을 "서버 전체"가 아니라 "`ledger` 가 만질 수 있는 것"으로 좁히는 것
    - **비번을 만들지 않는다** — `/etc/shadow` 두 번째 칸이 `!`(잠김). systemd 는 root 로 돌다가 `User=ledger` 로 **내려가는** 것이라 인증 절차 자체가 없다. 비번을 만들면 막아지는 경로 없이 관리할 비밀만 하나 는다 — ADR-043 에서 MySQL root 에 내린 판단과 같은 논리
    - 셸이 없어도 `sudo -u ledger <명령>` 은 된다. 막히는 건 대화형 로그인(`su - ledger`)뿐이라 디버깅에 지장 없음
    - `/etc/passwd` 의 `x` 는 "비번 없음"이 아니라 "해시는 `/etc/shadow` 에 있다"는 표시. `passwd` 는 누구나 읽을 수 있어서 해시를 root 전용 파일로 옮긴 결과
  - 이 단계가 못 막는 것 — 앱이 뚫리면 `ledger_db` 안은 그대로 털린다 (ADR-043 "감수"). 전용 유저는 DB **밖의** 서버를 지키는 장치다
  - 설치 후 `free -h` available **915Mi** (2-2 직후 879Mi). JRE 는 깔아둔 것만으론 메모리를 쓰지 않는다 — 실제 판단은 2-5 에서 앱을 올린 뒤
- **2-4 systemd — ✅ 2026-09-18** — `ExecStart` 는 `java -jar`, `Restart=always`(운영 3종의 "자동 재시작"이 여기서 같이 해결), `User=ledger`.
  환경변수는 `EnvironmentFile=`(권한 600)에 — `SPRING_PROFILES_ACTIVE=prod` / `DB_USERNAME` / `DB_PASSWORD` / `JWT_SECRET`
  - **자리 결정** — jar 는 `/opt/ledger/`, 설정은 `/etc/ledger/ledger.env`
    - `/opt` — 패키지 매니저 밖에서 온 자체 완결 소프트웨어의 자리(FHS). apt 로 깐 MySQL·Java 는 `/usr` 밑, 직접 올리는 jar 는 `/opt`
    - `/etc` — 설정의 자리(nginx → `/etc/nginx`, MySQL → `/etc/mysql`). **나누는 이유는 수명이 다르기 때문** — jar 는 배포마다 갈아끼우고 비밀값은 서버가 사는 동안 그대로. 같은 폴더에 두면 3단계 CD 가 `/opt/ledger` 를 비우다 비밀값을 같이 지우는 사고가 난다
    - `/etc/default/ledger` 가 아닌 이유 — 그 자리는 배포판 패키지 관례. 직접 올린 앱은 자기 이름의 디렉토리를 갖는다
    - 실무 규모에선 파일 대신 Secrets Manager·Parameter Store 에서 기동 시 받아온다. 한 대짜리엔 과함 — "지금은 파일 + 600, 서버가 늘면 Parameter Store"
  - **소유권** — `ledger.env` 는 `root:root 600`. systemd(PID 1, root)가 유닛을 준비하며 파일을 읽고 값을 환경변수로 넘긴 **뒤에** `User=ledger` 로 내려가므로 앱 계정은 이 파일을 읽을 필요가 없다. `/opt/ledger` 는 올리는 쪽(지금은 `ubuntu`, 3단계부터 배포 계정) 소유 755, jar 644 — **앱은 자기 실행 파일을 고칠 수 없어야 한다**(뚫린 앱이 jar 를 바꿔치기하는 경로 차단)
  - `JWT_SECRET` 형식 — 코드가 `Keys.hmacShaKeyFor(secret.getBytes())` 로 문자열 바이트를 그대로 키로 쓴다(2026-09-18 grep). 32바이트 이상이면 HS256, 64바이트 이상이면 HS512. `openssl rand -base64 48`(64글자) 로 만들면 된다. base64 는 `#`·공백이 없어 EnvironmentFile 에 따옴표 없이 넣어도 안전
  - **실측 (2026-09-18)** — `/etc/ledger` `drwxr-xr-x root root` / `/opt/ledger` `drwxr-xr-x ubuntu ubuntu` / `ledger.env` `-rw------- root root`.
    값 4개는 `sudo awk -F= '{print $1, length($2)}'` 로 **이름과 길이만** 확인(값은 화면·채팅에 띄우지 않는다) — `SPRING_PROFILES_ACTIVE 4` / `DB_USERNAME 10` / `DB_PASSWORD 32` / `JWT_SECRET 64`
    JWT 시크릿은 `openssl rand -base64 48`. `ledger.env` 는 `=` 양옆 공백·따옴표 없이, 값에 `#` 가 있으면 systemd 가 주석으로 보므로 그 줄만 큰따옴표
  - **유닛 파일** `/etc/systemd/system/ledger.service` — `Type=simple` / `User`·`Group=ledger` / `WorkingDirectory=/opt/ledger` / `EnvironmentFile=/etc/ledger/ledger.env` / `ExecStart=/usr/bin/java -jar /opt/ledger/ledger.jar` / `Restart=always` + `RestartSec=5` / `SyslogIdentifier=ledger` / `WantedBy=multi-user.target`
    - `After=network-online.target mysql.service` 만 쓰고 **`Requires=` 는 안 쓴다** — `Requires` 는 "MySQL 이 죽으면 나도 죽어라" 까지라 MySQL 재시작마다 앱이 같이 내려간다. 순서만 정하고 접속 실패는 `Restart=always` 가 재시도로 흡수
    - `RestartSec=5` 가 없으면 기동 실패 시 초당 수십 번 재시도하며 로그를 태운다
    - `ExecStart` 는 **절대경로 필수** — systemd 는 셸이 아니라 `PATH` 를 보지 않는다. `/usr/bin/java` 는 `/etc/alternatives/java` 심볼릭 링크(데비안 alternatives)지만 systemd 가 따라간다. 나중에 다른 JDK 를 깔면 이 스위치가 옮겨갈 수 있음
    - **jar 이름을 `ledger.jar` 로 고정** — 빌드 산출물은 `ledger-0.0.1-SNAPSHOT.jar`. 버전이 파일명에 박히면 버전 올릴 때마다 유닛 파일을 고쳐야 한다. 올릴 때 이름을 바꿔 두면 유닛 파일은 영원히 그대로 (ETC)
    - 보안 4줄 — `NoNewPrivileges`(도중 권한 상승 차단) / `PrivateTmp`(전용 `/tmp`) / `ProtectSystem=full`(`/usr`·`/boot`·`/etc` 읽기 전용) / `ProtectHome`(`/home` 비노출). 앱은 자기 디렉토리 읽기 + MySQL 접속이 전부라 넷 다 걸어도 지장 없다. 실무 유닛 파일의 기본 세트
    - `EnvironmentFile` 은 `ProtectSystem` 과 무관 — systemd(root)가 샌드박스를 걸기 **전에** 읽어 환경변수로 넘긴다
  - **등록** — `systemd-analyze verify`(우리 파일 지적 0줄 = 통과. `xfs_scrub_all` 경고는 우분투 기본 유닛이라 무시) → `daemon-reload`(디스크를 다시 읽어라. 유닛 파일 만들거나 고칠 때마다) → `enable`(부팅 시 자동 기동 등록 — `multi-user.target.wants/` 에 심볼릭 링크) → `is-enabled` 가 `enabled`
    - **`enable` ≠ `start`** — `enable` 은 "앞으로 부팅할 때마다", `start` 는 "지금". 별개라서 하나만 하면 반쪽이 된다. 환경변수는 `start` 하는 순간 읽힌다
    - 여기서 **운영 3종의 "자동 재시작"이 닫혔다** — 앱이 죽으면 `Restart=always`, 서버가 재부팅되면 `enable`
  - 남은 것 — jar 가 없어 `start` 는 2-5 에서
  - ADR-046 후보 — "설정은 `/etc`, 실행 파일은 `/opt`, 비밀값 파일은 root 소유" (문제 → 대안 → 근거 → 감수 로 정리)
- **2-5 수동 첫 배포 — ✅ 2026-09-18** (ADR-045 · 트러블슈팅 12)
  - 순서 — 맥에서 `./gradlew clean build` → `scp build/libs/ledger-0.0.1-SNAPSHOT.jar ubuntu@<EC2_HOST>:/opt/ledger/ledger.jar` → `sudo systemctl start ledger`
    - 목적지에 파일명을 적으면 **전송하면서 개명**된다 — 2-4 의 "유닛 파일은 `ledger.jar` 고정" 이 여기서 맞물린다
    - `-plain.jar`(약 84KB)는 의존성·부트로더가 없어 `java -jar` 로 뜨지 않는다. 올리는 건 약 40MB 실행 jar
    - 맥 셸에 `DB_USERNAME`·`DB_PASSWORD`·`JWT_SECRET` 이 없으면 `build` 의 테스트가 `PlaceholderResolutionException` 으로 실패한다. STS 는 Run Configuration 에 값이 있어 뜨지만 터미널 gradle 은 셸 환경변수만 본다 (환경변수 표의 "맥북 = 셸 환경변수"가 여기서 처음 실제로 필요해졌다)
  - **첫 기동은 500 이었다 — 테이블명 대소문자.** Flyway 는 19테이블을 다 만들었는데 앱은 `Table 'ledger_db.users' doesn't exist`.
    맥은 `lower_case_table_names=2`(비교 시 무시), 리눅스는 `0`(그대로 비교). V1 이 대문자로 만들고 매퍼가 소문자로 찾았다 → V1 52곳을 소문자로 (ADR-045)
    - V1 이 바뀌면 Flyway 체크섬이 어긋난다 → 맥·EC2 의 **빈 DB 를 각각 `DROP DATABASE` → `CREATE DATABASE`** 하고 재적용. **jar 도 다시 빌드**(V1 은 jar 안의 리소스다)
    - 맥에서 재적용 후 **146케이스 전부 통과** — 이게 EC2 리허설이었다 (ADR-044 때와 같은 절차)
  - **실측** — 프로파일 `prod` / `started by ledger in /opt/ledger` / Flyway `<< Empty Schema >>` → `Successfully applied 1 migration`(0.9초) / 테이블 **19개**(18 + `flyway_schema_history`) / 기동 9초
  - **타임존 3계층 완료** — journald 시각(OS)과 Spring 로그 시각(JVM)이 같은 `+09:00`, 서버 `date` 로 넣은 오늘 날짜의 거래가 `@PastOrPresent` 통과(201).
    오전 9시를 기다려 재현할 필요가 없었다 (ADR-026 의 "배포 시 확인" 항목 종료)
  - 기능 — 가입 201 / 로그인 토큰 168자 / 미분류 E·I 2개 자동 생성 / 거래 201(`transType` 서버 파생 `E`, `version` 0)
  - **운영 로그에 SQL 없음** — `journalctl -u ledger | grep -c "Preparing:"` = 0. 1단계 프로파일 분리가 실제로 일한다는 증거
  - 메모리 — 기동 전 available 906Mi → 기동 후 **608Mi**(앱이 약 300Mi). 300Mi 기준선 위라 **스왑은 만들지 않았다**
  - 재부팅 자동 기동 — `sudo reboot` 후 `active (running)` · Main PID 988(부팅 초기 번호) · `curl localhost:8080/api/users/login` 이 405(GET 이라 메서드 거부 = 앱이 살아서 응답).
    **여기서 운영 3종의 "자동 재시작"이 닫혔다** — 앱이 죽으면 `Restart=always`, 서버가 재부팅되면 `enable`
  - `DROP DATABASE` 는 그 DB 에 준 권한을 회수하지 않는다 — `SHOW GRANTS` 가 두 줄 그대로 (MySQL 사양). 지우기 전에 **앱을 먼저 `stop`** 한다
- **환경별로 넣어줄 값** — 코드가 읽는 환경변수는 `DB_USERNAME` / `DB_PASSWORD` / `JWT_SECRET` 3개가 전부 (2026-09-17 grep). 앱이 뜨는 컴퓨터 3대에 각각 넣는다

| | 맥북 | GitHub Actions | EC2 |
|---|---|---|---|
| 위 3개 | 셸 환경변수 (기존) | `ci.yml` 일회용 값 (기존) | `EnvironmentFile` — **새로 만든 값** |
| `SPRING_PROFILES_ACTIVE` | `local` (STS Profile 칸) | 없음 | `prod` |
| `QA_PASSWORD` | 셸 환경변수 (**작은따옴표로** — 트러블슈팅 15) | `ci.yml` 일회용 값 (로컬·운영과 다른 값) | 없음 (운영에선 QA 안 돌림) |

  - **세 환경의 값은 서로 달라야 한다** — `ci.yml` 의 `JWT_SECRET` 은 공개 저장소에 있다. EC2 가 같은 값을 쓰면 누구나 로그인 토큰을 위조할 수 있다. EC2 용은 새로 생성한 긴 랜덤 값
  - 나중에 늘어날 값 — 도메인·S3 버킷(5단계) / SES 발신 주소(비번 재설정) / Clova 키(Sprint 2) / 카카오맵 키(Sprint 5) / 네이버·구글 OAuth 키(배포 후 소셜 로그인). 그 기능을 만들 때 이 표에 한 줄씩
- 첫 배포는 손으로 — 로컬에서 `./gradlew build` → `scp` → `systemctl start`. 자동화(3단계) 전에 수동으로 한 번 성공시켜야 무엇을 자동화하는지 안다
- 검증 — `journalctl -u ledger` 에 `profile is active: "prod"` + SQL 로그 없음 / 가입·로그인·거래 등록 / 오전 9시 이전 "오늘" 거래가 400 안 나는가 / 인스턴스 재부팅 후 자동 기동

### 3단계 가이드 — CD 최소판 (게이트 없이)

- `ci.yml` 에 `deploy` 잡 추가. `needs: build` + **push to main 일 때만**(PR 에선 배포 금지) 조건
- build 잡이 만든 jar 를 artifact 로 넘겨 deploy 잡이 받는다 — **테스트한 그 jar 를 배포** (deploy 잡에서 다시 빌드하지 않는다)
- 주의 — `build/libs` 에 `-plain.jar` 도 같이 생긴다. 실행 jar 만 집거나 `build.gradle` 에서 plain jar 를 끈다
- GitHub Secrets — `EC2_HOST` / `EC2_USER` / `EC2_SSH_KEY`. **배포 전용 키쌍**을 새로 만든다 (내 접속용 키와 분리)
- EC2 쪽 — 배포 계정이 `systemctl restart ledger` **만** 비밀번호 없이 sudo 가능하게 (sudoers 한 줄)
- 흐름 — jar 전송 → restart → 헬스 확인(curl 로 200 이 올 때까지 대기, 안 오면 잡 실패)
- **여기서 결정할 것 — 헬스 확인 경로.** actuator 는 의존성에 있고 기본 노출은 `health` 하나뿐인데, `SecurityConfig` 의 `anyRequest().authenticated()` 가 `/actuator/**` 도 덮어서 토큰 없이는 **401** (2026-09-17 확인).
  대안: `/actuator/health` 만 permitAll / EC2 안에서 `localhost:8080` 으로 확인(밖엔 안 열림) / 정적 페이지 200 으로 대체. UptimeRobot(5단계)도 같은 경로를 쓰게 되므로 같이 정한다
  - 절대 하지 않을 것 — `management.endpoints.web.exposure.include` 에 `*` 나 `env`. `/actuator/env` 는 환경변수(DB 비번·JWT 시크릿)를 웹으로 보여준다
- 검증 — 화면 문구 하나 바꿔 push → Actions 초록불 → 배포 URL 에 반영

#### 진행 기록

- **3-1 헬스 확인 경로 — ✅ 2026-09-18** — `/actuator/health` **만** permitAll. 매처는 `EndpointRequest.to(HealthEndpoint.class)`
  - 문자열 경로(`"/actuator/health"`) 대신 매처를 쓴 이유 — `management.endpoints.web.base-path` 를 바꿔도 보안 설정이 따라온다. "health 엔드포인트를 연다"는 의도가 코드에 그대로 드러남
  - **Spring Boot 4.0 에서 두 클래스의 패키지가 옮겨졌다** — `o.s.b.security.autoconfigure.actuate.web.servlet.EndpointRequest` / `o.s.b.health.actuate.endpoint.HealthEndpoint`. 3.x 예제(`o.s.b.actuate.autoconfigure.security.servlet`·`o.s.b.actuate.health`)를 그대로 쓰면 컴파일 실패
  - 실측 — `/actuator/health` 200 `{"groups":["liveness","readiness"],"status":"UP"}` / **맥 MySQL 을 내리면 503 `DOWN`**(앱 프로세스는 살아 있는데 DB 가 죽은 상태를 잡아낸다 — 정적 페이지 200 방식이 못 잡는 것) / `/actuator` 401(링크 목록은 `LinksRequestMatcher` 라 안 열림) / `/actuator/env` 401 / `/api/transactions` 401 유지
  - 절대 하지 않을 것 — `management.endpoints.web.exposure.include` 에 `*` 나 `env`
  - 5단계에서 Nginx 가 `/actuator/` 는 막고 `/actuator/health` 만 통과시키게 (앱·Nginx 이중 방어). management 포트 분리(`management.server.port`)는 **하지 않는다** — 8080 자체가 밖에 안 열려 있어 막아지는 경로가 없고 Nginx 설정만 는다. 서버가 늘거나 actuator 를 더 열 때 재검토
- **3-2 배포 전용 키쌍 — ✅ 2026-09-18** — 맥에서 `ssh-keygen -t ed25519 -f ~/.ssh/ledger-deploy -C "github-actions-deploy" -N ""`
  - `-N ""`(passphrase 없음) — CI 엔 암호를 쳐 줄 사람이 없다. 키 자체는 무방비가 되는 대신 **계정 권한으로 좁혀서 벌충**한다 (3-3)
  - `ledger-admin` 을 재활용하지 않는 이유 — 그 키는 `ubuntu`(sudo 전권)에 붙는다. Secrets 에 넣는 순간 사본이 GitHub 쪽과 모든 워크플로 실행 환경에 생기고, 서드파티 액션 사고 하나로 서버 전체를 잃는다. 전용 키는 피해가 "jar 교체 + 재시작"에 갇힌다
  - pem 과 성격이 다르다 — pem 은 AWS 가 1회만 내려줘 분실 시 재발급 불가지만, **이 키는 분실·유출되면 새로 만들어 `authorized_keys` 만 갈아 끼우면 된다.** 그동안 내 SSH 접속은 안 끊긴다
- **3-3 배포 계정 + sudoers — ✅ 2026-09-18**
  - `deploy` 계정 — `useradd --create-home --shell /bin/bash`. `ledger`(nologin·홈 없음)와 달리 **셸과 홈이 있다** — SSH 로 들어와 파일을 놓고 명령을 실행해야 하기 때문. 비밀번호는 잠긴 채(`!`)라 키 없이는 못 들어온다
  - `.ssh` 700 / `authorized_keys` 600 은 권장이 아니라 **필수** — 더 넓으면 sshd 가 그 키를 통째로 무시하고 `Permission denied (publickey)` 를 준다
  - 공개키는 **클립보드를 거치지 않는다** — 맥에서 `ssh -i <PEM> ubuntu@<EC2_HOST> "sudo tee /home/deploy/.ssh/authorized_keys > /dev/null" < ~/.ssh/ledger-deploy.pub`.
    실제로 붙여넣기로 하다 클립보드에 남아 있던 **명령 문자열이 파일에 들어가** 1회 실패했다. 공개키는 한 줄이어야 하고 사람 손을 거치면 줄바꿈·오염이 끼어든다
  - `/opt/ledger` 소유권 `ubuntu` → `deploy` (2-4 의 "올리는 쪽 소유 755" 그대로). `/etc/ledger/ledger.env` 는 **여전히 root 600** — 배포 계정이 못 읽는다
  - sudoers — `/etc/sudoers.d/ledger-deploy`(440) 에 `deploy ALL=(root) NOPASSWD: /usr/bin/systemctl restart ledger` 한 줄.
    **문자열 그대로 매칭**이라 워크플로도 정확히 `sudo systemctl restart ledger` 로 불러야 한다 (`ledger.service` 로 쓰면 인자가 달라 거부)
    - 만들 땐 `echo ... | sudo tee` → `chmod 440` → `sudo visudo -c -f <파일>`(문법 검사 `parsed OK`). 대화형 `visudo` 편집기는 저장 실수가 나기 쉬워 피했다
    - `/etc/sudoers.d` 는 750 root 라 일반 계정은 **파일 존재 여부조차 못 본다**(`ls` 가 Permission denied — 정상. 공격자에게 권한 지도를 주지 않으려는 것)
  - 실측 — `sudo systemctl restart ledger` → `active` / `sudo systemctl stop ledger` **거부** / `sudo cat /etc/ledger/ledger.env` **거부**.
    **배포 키가 털려도 DB 비번·JWT 시크릿은 넘어가지 않는다**
  - 유닛 파일 mtime(21:03)이 2-4 의 `daemon-reload` 보다 나중이라 `changed on disk` 경고가 떠 있었다 → `daemon-reload` 로 해소. `deploy` 는 reload 권한이 없으므로 `ubuntu` 로 한다
  - **`systemctl status` 의 `active` 는 "앱이 떴다"는 뜻이 아니다** — restart 직후 `active (running)` 인데 Memory 1.9M(앱은 약 300Mi). `Type=simple` 은 **프로세스를 실행한 순간**을 성공으로 친다. 기동 실패해도 `Restart=always` 가 5초마다 다시 띄워 한동안 `active` 로 보인다.
    → 3-5 의 배포 흐름에 **헬스 200 대기 루프가 필요한 이유**

- **3-4 CD 권한 — 진행 중 (2026-09-19)** — 보안그룹 22번이 "내 IP 만" 이라 Actions 러너가 SSH 를 못 붙는다.
  **B안 채택** — 배포할 때만 러너 IP 를 22번에 열고 끝나면 닫는다. 그 권한은 **OIDC** 로 받는다 (GitHub 에 장기 액세스 키를 두지 않는다)
  - **왜 OIDC 인가** — 액세스 키를 Secrets 에 넣으면 만료 없는 비밀이 하나 생긴다. OIDC 는 GitHub 이 실행마다 "나는 이 저장소의 이 브랜치다" 라는 서명된 토큰을 발급하고, AWS 가 그걸 검증해 **1시간짜리 임시 자격**만 내준다. 훔쳐 갈 장기 키 자체가 없다 (aws-인프라.md "루트 액세스 키를 만들지 않는다" 와 같은 방향)
  - **문이 두 개다** — ① AWS API(보안그룹 규칙 추가·삭제) = OIDC 역할 ② SSH(jar 교체·재시작) = `ledger-deploy` 키 + `deploy` 계정(3-2·3-3). 둘은 서로 독립이고, 각자 자기 몫만 할 수 있다
  - ① IAM **ID 제공업체**(OIDC) 등록 ✅ — `token.actions.githubusercontent.com` / 대상 `sts.amazonaws.com`. 이것만으론 권한이 0
  - ② IAM **정책** `ledger-deploy-sg-ssh` ✅ — `ec2:AuthorizeSecurityGroupIngress` · `ec2:RevokeSecurityGroupIngress` 두 개만, 리소스는 `launch-wizard-1` 보안그룹 **하나의 ARN**(`<ACCOUNT_ID>`·`<SG_ID>` 는 저장소 밖 개인 보관처)
    - 콘솔의 "종속 권한이 선택되지 않았습니다 — requires 1 more action" 경고는 **무시해도 된다**. 열어 보니 `ec2:CreateTags` 였고, 설명에 "요청에 `TagSpecifications` 가 포함될 때만" 이라는 조건이 붙어 있다. 우리는 규칙에 태그를 달지 않는다.
      **권한은 좁게 시작하고 실제로 `AccessDenied` 가 나면 그때 한 줄 늘린다** — 판정은 3-6 검증에서 바로 드러난다
    - **감수 — "22번 포트만" 으로는 좁혀지지 않는다.** IAM 조건 키에 포트가 없어서, 이 역할은 그 보안그룹 하나에 한해 어떤 포트든 열고 닫을 수 있다. 다른 보안그룹·EC2 자체는 못 건드린다.
      포트까지 막으려면 SSH 를 아예 쓰지 않는 길(SSM Session Manager)로 가야 하는데, EC2 에 에이전트·인스턴스 역할을 더해야 해서 지금 규모엔 과하다. 서버가 늘거나 배포 경로가 늘면 재검토
  - ③ IAM **역할** `ledger-github-deploy` ✅ — 웹 자격 증명 방식. 붙인 권한은 `ledger-deploy-sg-ssh` 하나
    - **신뢰 정책 실측** — `Action: sts:AssumeRoleWithWebIdentity` / `:aud` 는 `StringEquals` `sts.amazonaws.com` /
      `:sub` 는 `repo:devkyumin04/ledger:ref:refs/heads/main` (**main 한정**). 콘솔이 조직·저장소·브랜치 칸을 채우면 이 문자열을 만들어 준다
    - **여기서 반드시 눈으로 볼 것** — `:sub` 가 `repo:devkyumin04/ledger:*` 같은 와일드카드로 들어가면 **PR 브랜치·포크에서도 이 역할을 맡을 수 있다**(배포 권한이 열리는 셈).
      콘솔은 연산자로 `StringLike` 를 쓰는데, 값에 `*`·`?` 가 없으면 `StringEquals` 와 동작이 같다 — **연산자가 아니라 값에 와일드카드가 있는지를 본다**
    - 콘솔이 같은 `sub` 값을 배열에 두 번 넣었다. 평가 결과엔 영향 없어 그대로 뒀다
    - deploy 잡에 `environment:` 를 쓰면 `sub` 형식이 `repo:...:environment:<이름>` 으로 바뀌어 거부된다 — 이번 워크플로엔 쓰지 않는다
    - 남은 것 — **3-5 에서 역할을 실제로 맡아 봐야 신뢰 정책이 맞는지 증명된다.** 여기까지는 "설정했다" 까지다
  - ④ GitHub Secrets ✅ (2026-09-19) — **4개만**: `AWS_ROLE_ARN` · `AWS_SG_ID` · `EC2_HOST` · `EC2_SSH_KEY`. `AWS_REGION` · `EC2_USER`(`deploy`)는 `ci.yml` 에 그대로
    - 이름에 `AWS_` 접두사를 붙였다 — 워크플로에서 이름만 보고 무엇이 AWS 소관인지 구분된다
    - 기준은 CLAUDE.md "값 3단 분류"(2026-09-19 신설) — 비밀은 Secrets / **식별자도 이 저장소에선 Secrets**(공개 저장소는 Actions 로그가 공개고, Variables 는 로그에 그대로 찍힌다) / 공개 설정은 코드
    - 진짜 비밀은 `EC2_SSH_KEY` 하나. 개인키는 `pbcopy < ~/.ssh/ledger-deploy` 로 복사해 손을 거치지 않는다 (3-3 의 클립보드 오염과 같은 함정)
  - **전수 검사 (2026-09-19, 규칙 신설 직후)** — 설계-체크리스트 8번 확장판으로 실측
    - 저장소 — 추적 파일·**git 이력 50커밋 전체**에서 계정 ID / `sg-` / 개인키 / AWS 키 / GitHub 토큰 / JWT / IPv4 / 실제 이메일 **전부 0건**. 이력 속 yml 의 비밀값은 처음부터 `${ENV}` 였다. 추적된 적 있는 `.pem`·`.env`·로컬환경 0
    - AWS — 루트 액세스 키 0 · MFA 1 · 콘솔 "보안 권장 사항 0" / IAM 사용자 0 · 역할은 서비스 연결 3 + `ledger-github-deploy` / 보안 그룹 인바운드 3줄(22 = `/32` 하나, 80·443 = 전체) — 문서와 일치
    - GitHub — secret scanning · push protection 켜짐 / Actions 기본 권한 read / 포크 PR 첫 기여자 승인 요구. Dependabot alerts 는 꺼져 있음(비밀값과 무관, Sprint 1 리뷰 때 판단)
    - 서버 안(`ledger.env` 600 · sudoers · 계정)은 이번에 다시 보지 않았다 — 2-4·3-3 의 실측이 최신
  - **3-6 검증 — 1차 실패와 원인 (2026-09-20)** — deploy 잡이 **"AWS 임시 자격 받기"에서 3회 연속 실패**.
    `Not authorized to perform sts:AssumeRoleWithWebIdentity` (12회 재시도 후 포기)
    - **이 에러가 가리키는 범위** — GitHub 은 토큰을 정상 발급했고(= `id-token: write` 는 동작) AWS 가 거절한 것. 즉 **권한 정책이 아니라 신뢰 정책** 문제로 좁혀진다. 정책은 평가되지도 않는다
    - 콘솔 실측으로 지운 용의자 — 공급자 존재·audience(`sts.amazonaws.com`) / 역할 존재 / 신뢰 정책 `aud` / `sub` 문자열 / Principal. **전부 정상이었다**
    - **AWS 는 역할이 존재하지 않아도 같은 메시지를 준다**(존재 여부를 알려주지 않으려는 설계) → `AWS_ROLE_ARN` 을 의심해 덮어썼지만 여전히 실패
    - **임시 디버그 스텝으로 확정** — 토큰 payload 의 클레임과 ARN 길이만 출력(토큰 자체는 안 찍음). 밖에서는 시크릿이 `***` 로 가려지고 `with:` 블록도 접혀 있어 볼 방법이 이것뿐이었다
    - **원인 — GitHub 이 보내는 `sub` 에 숫자 ID 가 박혀 있다**
      + 실제: `repo:devkyumin04@<OWNER_ID>/ledger@<REPO_ID>:ref:refs/heads/main`
      + 콘솔이 자동으로 채워준 값: `repo:devkyumin04/ledger:ref:refs/heads/main`
      + 나머지(`aud`·`repository`·`ref`·`iss`·ARN 길이 51)는 전부 정답이었다
      + **이름 대신 숫자 ID 를 쓰는 이유** — 저장소 이름은 바꾸거나 지우고 같은 이름으로 다시 만들 수 있지만 ID 는 불변이다. 이름 선점으로 남의 역할을 맡는 경로가 막힌다. **더 안전한 형태이고, AWS 의 거절은 정상 동작이었다**
      + 콘솔 UI·공식 예제는 아직 이름 형태를 채워 준다 — **자동으로 채워준 값이 실제로 오는 값과 다를 수 있다**
    - **조치** — 신뢰 정책 `sub` 를 `StringEquals` 배열로, **두 형태 모두** 허용. 둘 다 "이 저장소의 main" 으로 똑같이 좁혀져 있어 느슨해지지 않는다. 와일드카드는 쓰지 않았다
    - **배운 것 (일반화)** — 밖에서 안 보이는 값이 원인일 때는 **추측을 늘리지 말고 그 값을 찍는 임시 스텝**을 넣는다. 찍을 땐 비밀 자체가 아니라 **길이·접두/접미·파싱된 클레임**만 (ARN 길이 51 하나로 시크릿 의심이 끝났다)
    - 실패한 3회 모두 ② 에서 멈춰 **서버·보안그룹은 건드려지지 않았다**(③~⑦ skipped, ⑧ 닫기는 `always` 라 돌았지만 열린 게 없어 할 일 없음)
  - **3-5 워크플로 작성 ✅ (2026-09-19~20)** — `ci.yml` 에 artifact 업로드 + `deploy` 잡
    - 흐름 — jar 받기 → OIDC 임시 자격 → 러너 IP 확인(`checkip.amazonaws.com`, 예비 `api.ipify.org`, `::add-mask::` 로 로그 마스킹) → 22번 열기(같은 규칙 먼저 지우는 자기 정리) → 키 파일 600 → `scp`(`.jar.new`) → **옛 jar 를 `.prev` 로 복사** → `mv` → `sudo systemctl restart ledger` → 헬스 200 대기(3초×30, 실패 시 `journalctl` 50줄) → `if: always()` 로 22번 닫기
    - `needs: build` + main push 만 / `permissions: id-token: write, contents: read` / `concurrency: deploy-prod`(배포가 겹치면 서로의 보안그룹 규칙을 지운다. 진행 중 배포는 취소하지 않음 — 중간에 죽으면 22번이 열린 채 남는다)
    - artifact 패턴 `*-SNAPSHOT.jar` — `-SNAPSHOT-plain.jar` 는 안 걸린다. `if-no-files-found: error`
    - 헬스는 **서버 안에서 `localhost:8080`** — 8080 은 밖에 안 열려 있고 Nginx 는 5단계
    - `paths-ignore: docs/**, **.md` — 문서만 바꾼 push 로 운영을 재시작하지 않는다
    - 새 액션 3개는 커밋 SHA 고정. build 잡의 `checkout@v4`·`setup-java@v4` 는 **일부러 안 건드림**(CD 커밋에서 잘 돌던 빌드를 같이 바꾸면 실패 원인이 섞인다 — 로드맵 4번)
    - 롤백 — `ssh deploy@<EC2_HOST>` → `cp /opt/ledger/ledger.jar.prev /opt/ledger/ledger.jar && sudo systemctl restart ledger`
  - 3-5 에서 같이 정할 것
    + **닫기는 `if: always()`** — 배포가 실패해도 22번이 열린 채 남으면 안 된다. 그래도 러너가 강제 종료되면 규칙이 남을 수 있으므로, **여는 단계에서 같은 규칙을 먼저 지우고 시작**하게 만든다(자기 정리)
    + **워크플로 하드닝** — deploy 잡에 `permissions: { id-token: write, contents: read }`(OIDC 토큰 발급에 `id-token` 이 필요하고, 나머지는 읽기만) / 개인키는 파일로 쓰고 600, 로그에 찍히는 명령(`set -x`, `cat`) 금지 / `aws-actions/configure-aws-credentials` 는 커밋 SHA 고정
    + **러너 IP 를 무엇으로 알아내는가** — 보통 `curl https://api.ipify.org` 같은 외부 서비스를 쓴다. 배포가 남의 서비스 가용성에 묶인다는 뜻이라 대안·실패 처리를 같이 정한다

- **3-6 검증 ✅ (2026-09-20) — 3단계 CD 최소판 완료**
  - 실행 #48 전 스텝 성공, 약 35초(자격 1 · 22번 열기 6 · 전송·교체·재시작 11 · **헬스 대기 11** · 닫기 2). 기동 9초라 헬스 루프가 실제로 기다렸다 — `active` 만 믿었으면 뜨기 전에 성공 처리됐을 것
  - 서버 실측 — `ledger.jar` 17:37(deploy 소유) / `ledger.jar.prev` = 09-18 수동 배포본 / `Active since 17:37:31` / 헬스 `UP` / 보안 그룹 인바운드 **3줄로 복귀**(22번 닫힘 확인)
  - **바꾼 코드가 실제로 반영되는가** — 메인 화면 개편(`a301c04`) push → 서버에서 `curl -s localhost:8080/views/main.html | grep -c "이렇게 씁니다"` = 1
  - Actions 로그 끝의 `Post AWS 임시 자격 받기` 는 순서가 뒤바뀐 게 아니다 — 액션은 main(스텝 차례)과 post(잡 끝에 역순으로)를 같이 갖고 있고, post 가 심어둔 임시 자격을 걷어낸다. `Post JDK 설정`·`Post 체크아웃`도 같은 구조
  - `systemctl status` 의 "unit file changed on disk" 경고가 다시 보였다 — 동작엔 영향 없음. `ubuntu` 로 `sudo systemctl daemon-reload`
- **3-7 보강 ✅ (2026-09-20)** — CD 가 1회 성공한 직후라 이제 붙였다 (같이 넣었으면 첫 실패의 원인 후보가 늘었을 것)
  - **호스트 키 고정** — Secret `EC2_HOST_KEY`(`ssh-ed25519 AAAA…` 한 줄, 81바이트) → 워크플로가 `known_hosts` 를 미리 쓰고 `StrictHostKeyChecking=yes`(3곳).
    값은 `ssh-keyscan` 이 아니라 **내 맥의 `known_hosts` 에서** 꺼냈다(`ssh-keygen -F <EC2_HOST>`) — keyscan 은 지금 네트워크가 돌려주는 값을 그냥 받는 것이라, 검증하려는 걸 검증 없이 가져오는 셈. 2-1 첫 접속 때 믿기로 한 값을 쓴다.
    **서버를 새로 만들면 지문이 바뀐다 → 배포가 `Host key verification failed` 로 멈춘다(정상). 그때 Secret 을 갱신**
  - **호스트 키 1차 실패 (CI #50)** — `No ED25519 host key is known for *** and you have requested strict checking` → `Host key verification failed`.
    build 는 새 액션 버전으로 통과, deploy 는 전송 직전에 멈춤(22번은 `always` 로 닫힘, 서버 무변경). **ssh 는 `known_hosts` 의 형식이 틀린 줄을 조용히 무시**해서 "키가 없다"고만 말한다 — 값이 잘렸는지 밖에서 안 보인다.
    → 워크플로에 **상시 검사**를 넣었다: `ssh-keygen -l -f known_hosts` 가 실패하면 길이(정상 80)와 앞 11글자를 찍고 중단. 임시 디버그가 아니라 남겨 둘 검사다(3-6 의 "밖에서 안 보이는 값은 길이·접두를 찍는다" 를 미리 심어 둔 것)
    **원인은 값이 아니라 확인 절차였다** — 키를 `pbcopy` 한 뒤, 채팅에서 확인 명령(`pbpaste | wc -c`, 정확히 15글자)을 복사하는 순간 클립보드가 덮어써졌다. 두 방법(known_hosts·서버 원본) 모두 "15"가 나온 이유. → 복사와 확인을 한 줄로: `… | tee >(pbcopy) | wc -c` (80 출력 + 클립보드 유지). **클립보드에 값을 넣은 뒤엔 다른 걸 복사하지 않는다**
    **CI #51 ✅ (`e636d42`)** — 지문 검사 통과 → `StrictHostKeyChecking=yes` 로 접속 → 전송 10초 · 헬스 11초 · 22번 닫힘. 호스트 키 검증이 실제로 동작한다
  - **액션 전부 SHA 고정** — build 잡의 `checkout`(v4 → v7.0.1) · `setup-java`(v4 → v6.0.1)까지. 태그 고정 0건
  - **Dependabot** — `.github/dependabot.yml`(github-actions, weekly). SHA 고정의 단점(수동 업데이트)을 없앤다. PR 에선 build 만 돌고 deploy 는 main push 조건이라 안 돈다

### 배포 · 운영 보강 로드맵 — 시점별 (2026-09-19)

CD 를 만들며 드러난 허점들. **한꺼번에 하지 않는다** — 시점을 정해 흩어 놓고 그때 꺼낸다 (ADR-041 과 같은 방식).
"왜 그 시점인가" 가 항목마다 있다. 이해용 설명은 위 "꼭 짚고 넘어갈 것", 여기는 **실행 목록**.

| # | 할 일 | 시점 | 왜 그때인가 | 크기 |
|---|---|---|---|---|
| 1 ✅ | **롤백용 `.prev` 백업** (2026-09-20 워크플로에 반영) — 덮어쓰기 전에 옛 jar 를 복사, 실패 시 SSH 한 줄로 복구 | **3-6 직전(지금)** | 지금 없으면 첫 배포가 실패했을 때 서비스가 내려간 채로 남는다. 넣는 비용이 워크플로 한 줄 | 한 줄 |
| 2 ✅ | **main 브랜치 보호** (2026-09-20, 룰셋 `protect-main` — active · 기본 브랜치 · `deletion` + `non_fast_forward` · 우회 0명, API 실측) — force push 금지 · 브랜치 삭제 금지. ("상태 체크 통과 필수"는 **걸지 않는다** — main 에 직접 push 하는 1인 흐름에선 새 커밋에 아직 체크 결과가 없어 push 자체가 막힌다. PR 흐름으로 바꿀 때 같이) | **3-6 직전(지금)** | 오늘부터 **push = 운영 반영**이다. 그 전까지는 빌드만 돌았으니 없어도 됐다 | 설정 5분 |
| 3 ✅ | **호스트 키 검증** (2026-09-20) — `ssh-keyscan` 결과를 `EC2_HOST_KEY` Secret 에, `known_hosts` 고정 + `StrictHostKeyChecking=yes` | **3-6 성공 직후** | 지금 같이 넣으면 첫 배포 실패 시 원인 후보가 넷(OIDC·SSH 키·호스트 키·액션 버전)이 된다 | Secret 1개 + 3줄 |
| 4 ✅ | **액션 SHA 고정 마무리** (2026-09-20) — build 잡의 `checkout`·`setup-java` + **Dependabot 켜기** | **3-6 성공 직후** | 같은 이유. Dependabot 이 SHA 올리는 PR 을 만들어 주므로 고정의 단점(수동 업데이트)이 사라진다 | 2줄 + 설정 |
| 5 ✅ | **게이트 (146케이스)** (2026-09-20 — 일부러 깨서 deploy 가 skipped 되는 것까지 확인, CI #54) | **4단계** | ADR-042 — CD 가 게이트 없이 1회 성공한 직후. 같이 만들면 실패 원인이 안 갈린다. `_lib.sh` 비대화 모드가 선행 | 4단계 전체 |
| 6 | **forward-only 마이그레이션 원칙** — 컬럼 삭제는 "안 쓰게 만들기 → 다음 배포에서 삭제" 두 단계 | **V2 를 처음 쓸 때(Sprint 2 스키마 변경 직전)** | 롤백해도 스키마는 안 돌아온다. 규칙은 **첫 V2 를 쓰기 전에** 정해야 의미가 있다 | 규칙 + ADR |
| 7 | **무중단 배포** — 새 jar 를 8081 로 띄우고 헬스 통과 후 Nginx upstream 교체 + `reload` | **5단계 Nginx 이후 + 메모리 여유 생긴 뒤(t4g.medium)** | Nginx 가 없으면 트래픽을 옮길 대상이 없다. 지금 막는 건 구조가 아니라 메모리(앱 1개 약 300Mi / available 608Mi) | 중간 |
| 8 | **SSM Session Manager 전환** — 22번을 영구히 닫는다 | **5단계 이후, 또는 배포 경로·서버가 늘 때** | "열었다 닫는" 건 우회책이다. 다만 지금 바꾸면 방금 만든 CD 를 통째로 다시 만드는 셈 — 한 번 굴려 보고 판단 | 큼 |
| 9 | 롤백을 버튼으로 (`workflow_dispatch` 로 이전 아티팩트 재배포) | 배포가 잦아지면 (선택) | 1번으로 복구는 되지만 손이 간다. 자주 쓰게 되면 그때 | 작음 |
| 10 ✅ | **`ledger-admin.pem` 을 `~/.ssh/` 로 옮기기** (2026-09-20 완료 — pem `-r--------` 400 · 개인 문서와 서버 접속 `.command` 는 `~/private/` · `.command` 안의 경로도 `~/.ssh/` 로 갱신, 실측. 남은 것: DBeaver SSH 탭 경로) | 아무 때나 (5분) | 지금은 `~/Documents` 에 있다. 2026-09-20 Finder 실측 — 창 제목 "문서 — 로컬", 사이드바에서 문서가 즐겨찾기 아래 = **iCloud "데스크탑 및 문서" 동기화는 꺼져 있어 당장 위험은 없다.** 다만 나중에 동기화를 켜면 그 순간 같이 올라가고, 문서 폴더는 앱이 가장 쉽게 접근 권한을 받는 자리다. 옮길 때 경로를 같이 고칠 곳 — 서버 접속 `.command` · `~/.ssh/config` · DBeaver SSH 탭 · 개인 문서 | 작음 |
| 11 | **백업 실패 알림** — UptimeRobot heartbeat(성공할 때마다 핑, 안 오면 알림) | **5-6 (다음)** | 지금은 백업이 조용히 멈춰도 모른다. 5-5 에서 남은 표준 중 가장 큰 구멍 | 스크립트 한 줄 + 설정 |
| 12 | **PITR** — binlog 보관으로 RPO 24시간 → 분 단위 | 실사용자가 생기면 | 지금은 하루치를 잃어도 잃을 게 없다(ADR-050 감수) | 중간 |
| 13 | 백업을 **별도 AWS 계정**(또는 다른 리전)으로 | 실사용자·결제가 붙으면 | 루트가 털리면 서버와 백업이 같이 사라진다. 지금은 루트 MFA + IAM 사용자 0명으로 확률을 낮춘다 | 중간 |
| 14 | 업로드 전 암호화(gpg 등) | 실사용자가 생기면 | 버킷을 읽을 수 있는 사람은 평문을 본다(지금은 루트뿐). 키를 잃으면 백업 전체를 못 쓰는 위험이 더 크다 | 작음 + 키 보관 |
| 15 | 전용 백업 MySQL 계정(읽기 권한만) | 서버가 늘거나 백업 방식을 바꿀 때 | 한 대에선 root 와 막아지는 경로가 같다(ADR-050). 실무 기본형이고, 스크립트 실수가 DB 를 못 건드리게 하는 안전장치 | 작음 |

- 1·2 를 먼저 하는 이유는 **되돌릴 수 없는 사고를 막는 것**이고, 3·4 는 **아직 일어나지 않은 공격을 막는 것**이라 순서가 뒤다
- 7·8 은 "지금 할 수 없는 것" 이 아니라 **"지금 하면 비싼 것"** 이다. 면접에서도 이 구분을 말할 수 있으면 된다

### 5단계 메모 — 도메인은 HTTPS 직전에 산다

- IP 주소만으로는 뒤 기능이 막힌다 — HTTPS 인증서(사실상 도메인 필요) / PWA 설치(HTTPS 필수) / 카카오맵·네이버·구글 콘솔 등록 / Refresh 쿠키 `Secure`(ADR-010)
- 구조 — 사용자 → Nginx(80 은 443 으로 돌려보냄, 443 에서 인증서 처리) → `localhost:8080`. 보안 그룹에 80·443 만 열어둔 이유 (aws-인프라.md)
- 도메인이 생기면 `application-prod.yml` 을 처음 만들 시점일 수 있다 (쿠키 `Secure`, CORS 등 운영 전용 값 — ADR-042)

#### 진행 기록 (5단계)

- **5-1 도메인 ✅ (2026-09-21) — `dotoree.app`** (Cloudflare Registrar, 연 $14.20, 자동 갱신)
  - **등록기관 Cloudflare** — 원가 판매라 갱신가가 안 뛴다 · WHOIS 가림 기본 · DNS 화면이 같은 곳. 감수: 네임서버를 Cloudflare 밖으로 못 바꾼다(Route 53 으로 옮기려면 도메인 이전 — 등록 후 60일 잠김). A·CNAME·TXT 몇 줄이면 되는 지금 규모엔 무관
  - **이름** — 가계부가 드러나는 2단어 영어 조합(`snapledger`·`homeledger` …)은 `.com`·`.app` 모두 선점. 특히 `ledger` 는 같은 이름의 암호화폐 지갑 회사 때문에 거의 다 잡혀 있다.
    기능을 설명하는 이름 대신 **서비스가 이름에 뜻을 채우는 쪽**(토스·당근과 같은 방식)으로 바꿨다. 도토리 = 다람쥐가 모으는 것 + 싸이월드 화폐
    - 후보는 등록 가능 여부와 함께 **같은 이름의 앱·서비스가 있는지**를 먼저 검색했다 — `찰칵`(찰칵장부 앱), `receipi`(일본 영수증 가계부 앱), `wonlog`(쇼핑몰)가 여기서 탈락
    - 철자 — 표준 표기 `dotori` 는 선점, `dotoree` 는 변형이다. 말로 불러주면 받아적기 어렵다 → **주소는 링크로 건넨다**
  - **TLD `.app`** — `dotoree.com` 은 원래 주인이 있고 만료 후 redemption(유예) 기간이라 살 수 없었다(RDAP 조회. 공개 검색 페이지는 "구매 가능"으로 잘못 보여줬고 로그인한 대시보드가 맞았다).
    `.app` 은 브라우저에 **HSTS preload** 로 박혀 있어 HTTPS 가 아니면 아예 안 열린다 — 돈·개인정보를 다루는 서비스라 오히려 맞다. 대신 5-2(Nginx 80)는 도메인이 아니라 `curl` 로 확인한다
  - **DNS** — `A @` → 탄력적 IP, `CNAME www` → `dotoree.app`, 둘 다 **DNS only(회색 구름)**. 확인 `dig +short` 두 이름 모두 IP(출력은 IP 라 기록하지 않음)
  - 값 분류(ADR-048) — 도메인 이름은 **공개 설정**(yml·문서에 그대로). Cloudflare 계정·2FA 백업 코드는 비밀(개인 보관처). 등록자 연락처는 ICANN 규정상 실정보, WHOIS 엔 Cloudflare 가 가려서 내보낸다
- **5-2 Nginx ✅ (2026-09-21)** — apt `nginx` **1.28.3**. 설치만으로 `enabled`. 메모리 8.3M, 설치 후 available 651Mi(스왑 불필요 그대로)
  - 설정 `/etc/nginx/sites-available/ledger` → `sites-enabled` 에 링크, 기본 `default` 링크는 제거(원본은 남음). 켜고 끄는 건 링크 — systemd `enable` 과 같은 구조
  - **문지기 블록** — `listen 80 default_server; server_name _; return 444;`. 모르는 이름(IP 직접·스캐너)은 응답 없이 끊는다. 이게 없으면 블록이 하나뿐일 때 Nginx 가 그걸 기본값으로 써서 IP 로 찔러도 앱이 대답한다
  - `proxy_set_header` 4줄(Host · X-Real-IP · X-Forwarded-For · X-Forwarded-Proto)을 **server 레벨**에 — location 이 늘어도 한 곳(ETC)
  - 적용은 `nginx -t && systemctl reload nginx`. `-t` 는 문법 검사, `reload` 는 연결을 끊지 않는다(무중단 배포도 결국 이 reload)
  - 실측 — `Host: dotoree.app` 302 / `www` 302 / IP 직접 `curl` exit 52(빈 응답) / 맥에서 `http://dotoree.app` 302 = 인터넷 → DNS → 탄력적 IP → 보안그룹 80 → Nginx → 앱 전 구간
  - 오판 한 번 — 채팅에 붙인 출력의 `www.` 가 링크 모양이라 파일 오염으로 봤다. 실제로는 채팅 표시만 그랬다 (트러블슈팅 16)
- **5-3 HTTPS ✅ (2026-09-21)** — apt `certbot` **4.0.0** + `python3-certbot-nginx`. Let's Encrypt, HTTP-01 인증
  - 전제가 앞 단계들이었다 — DNS 가 이 서버를 가리키고(5-1), 80 이 전체에 열려 있고, Cloudflare 프록시가 꺼져 있어야(회색 구름) Let's Encrypt 가 밖에서 80 으로 들어와 확인한다
  - **`certonly --dry-run` 먼저** — 실패가 시간당 5번 쌓이면 발급이 막힌다. 리허설은 스테이징이라 횟수에 안 잡힌다(게이트 먼저 → 배포와 같은 순서). 리허설과 본 발급은 계정이 따로라 이메일·약관을 두 번 묻는다
  - `certbot --nginx --redirect` — 인증서 발급 + 우리 server 블록에 443 설정 + 80→443 리다이렉트까지. 만료 **2026-12-20**, `certbot.timer`(하루 2번, 만료 30일 이내일 때만 갱신) · `renew --dry-run` 성공
  - Let's Encrypt 는 2025년에 만료 경고 메일을 끝냈다 — 만료를 막는 건 메일이 아니라 타이머 + UptimeRobot
  - **`Location: http://…` 문제** — Nginx 가 TLS 를 풀고 앱엔 http 로 넘겨서, 앱이 리다이렉트를 `http://` 로 만들었다. 브라우저는 `.app` 의 HSTS preload 가 몰래 https 로 바꿔 가려줬고 curl 이 드러냈다 → `server.forward-headers-strategy: native` 를 공통 yml 에(`129c48e`). 내부 주소(127.0.0.1·사설 IP)에서 온 `X-Forwarded-*` 만 믿는다. 프록시 없는 로컬·CI 에선 헤더가 없어 효과 없음 = 환경마다 다를 값이 아니다(ADR-042) → `application-prod.yml` 은 여전히 없음 (트러블슈팅 17)
  - 이게 없으면 절대 URL 을 만드는 곳(소셜 로그인 콜백·재설정 메일 링크)과 Refresh 쿠키 `Secure`(ADR-010)가 틀어진다
- **5-4 actuator 차단 · 보안 헤더 ✅ (2026-09-21)** (ADR-049)
  - Nginx 설정을 certbot 이 고친 형태에서 읽기 쉽게 다시 씀(백업 `ledger.bak-certbot`) — ① 80 문지기 444 ② **443 문지기 `ssl_reject_handshake on`**(IP 로 https 를 찌르면 인증서 = 도메인 이름도 안 보여준다. 전엔 첫 443 블록인 우리 앱이 인증서째 대답했다) ③ 80→443 `return 301`(certbot 의 `if` 두 개 대신) ④ 443 → 앱
  - `location = /actuator/health` 만 프록시, `location /actuator` 는 **404**(401 은 "뭔가 있다"는 힌트). 앱의 401 과 이중 방어. certbot 의 `ipv6only=on` 은 기본값이라 뺐다
  - 손으로 고친 파일도 certbot 이 인식하는지 `renew --dry-run` 재확인. 오염 검사는 `grep -c '](' ` = 0 (`[` 로 세면 IPv6 `[::]` 가 걸린다 — 트러블슈팅 16)
  - 실측 — health 200 / `/actuator`·`env`·`beans` 404 / `http://` 301 / IP 로 https `exit=35`(TLS 거부)
  - **보안 헤더는 실측부터** — 기획서는 "4종을 Nginx 에서" 였지만 HSTS·`X-Frame-Options: DENY`·`nosniff` 는 **Spring Security 기본값이 이미** 붙이고 있었다(5-3 으로 https 를 알게 되면서 HSTS 도). Nginx 에 또 넣으면 중복 → **CSP 만 Spring 에** 추가
  - CSP 를 켜기 전에 **우리 코드의 인라인부터 0 개로** — 브라우저는 우리 인라인과 공격자 인라인을 구분하지 못한다. `onclick="toggleTheme()"` → `theme.js` 의 `DOMContentLoaded` + `addEventListener` / 막대 `style="width:…"` → JS 의 `el.style.width`(CSP 는 HTML 의 style 속성을 막지 DOM 조작은 안 막는다) / Pretendard CDN 링크 8곳 → **시스템 폰트**(외부 출처가 폰트 하나뿐이었다. 버전 없는 CDN 주소는 내용이 바뀌어도 모른다 — 액션 SHA 고정과 같은 문제)
  - 정책 `default-src 'self'; object-src 'none'; base-uri 'none'; frame-ancestors 'none'; form-action 'self'` (`0c2ee1d`). 외부 출처 0 — 카카오맵 등이 붙을 때 한 줄씩 연다
  - **검증 — 공격 흉내** — 페이지에 `<img src=x onerror=…>` 와 `style=` 를 넣어 보니 실행 안 됨 + `script-src-attr`·`style-src-attr` 위반 이벤트. 우리 화면(🌓·막대·API 호출·6개 화면)은 정상. 로컬과 운영 둘 다
    - 주의 — 콘솔 도구로는 CSP 위반 문구가 안 잡혀 "콘솔 0건"은 증거가 못 됐다. 기능 동작 + `securitypolicyviolation` 이벤트로 확인했다
    - 게이트(curl 146)는 **브라우저에서만 드러나는 CSP 문제를 못 잡는다** — 화면을 바꾸면 사람이 한 바퀴 돈다
  - 곁가지 정리(`e459215`) — 로그인 없이 없는 주소가 401 텍스트였다 → 인증 진입점에서 API 가 아니면 `sendError(404)`(ADR-012 분리 그대로, `anyRequest().authenticated()` 기본 차단은 유지) / `UserDetailsServiceAutoConfiguration` 제외(운영 로그의 `Using generated security password` 제거. Boot 4 에서 패키지가 옮겨져 빌드된 jar 에서 경로를 확인하고 어노테이션 `exclude` 로 — 틀리면 컴파일에서 잡힌다)
  - 같은 날 미사용 코드 스캔 — 지울 건 `oauth2-client` 의존성 2줄뿐이었다(`dad5edf`, 소셜 로그인 직전에 다시). 빈 오류 span 5개는 폼 간격을 만들고 있어 뷰 대공사로
- **5-5 DB 백업 → S3 ✅ (2026-09-22)** (ADR-050)
  - 흐름 — systemd 타이머(매일 04:00 KST, `Persistent`) → `ledger-backup`(root) → `mysqldump --single-transaction` → 검사 2개 → 로컬 `/var/backups/ledger`(약 7일) → S3 `daily/`(30일 + 이전 버전 7일). 스크립트·유닛 정본은 저장소 `ops/`
  - 보관 — 로컬 7일은 빠른 복구·업로드 실패 대비(서버와 같이 죽으니 길게 둘 이유가 없다). S3 30일은 **늦게 알아챈 사고**(잘못된 배포가 조용히 망친 데이터를 정산 때 발견) 대비. 30일보다 길지 않게 — 탈퇴자 데이터가 백업에 그만큼 남는다(개인정보 처리방침엔 "최대 37일")
  - **새 비밀 0개** — MySQL 은 root `auth_socket`, S3 는 인스턴스 역할 `ledger-prod-ec2`. 서버에 둔 식별자는 버킷 이름 하나(`/etc/ledger/backup.env`)
  - 검사 — `gzip -t` 는 압축만 본다. 끝을 자른 덤프 흉내(`head -n -1 | gzip`)에 `gzip -t` 는 통과, 마지막 줄 `-- Dump completed` 검사가 잡았다 → 스크립트에 추가
  - 버킷은 **계정 리전 네임스페이스**(2026-03 기능) — 처음엔 글로벌 + 랜덤 접미사 + `--expected-bucket-owner` 로 계획했다가, 이름 선점·재사용을 원천 차단하는 이쪽으로 바꾸고 둘 다 뺐다. 버전 관리 ON · 수명 주기 `daily-expire`
  - 권한 — 정책 `ledger-backup-s3-put` = `PutObject` on `daily/*` 하나. 서버에서 실측: 목록·읽기·삭제·`daily/` 밖 쓰기 **거부** / `daily/` 쓰기·덮어쓰기 허용 → 콘솔 "버전 표시"에서 덮어쓴 파일의 이전 버전 보존 확인. IMDSv2 Required 확인(역할이 붙는 순간 SSRF 가 자격 증명 유출 경로가 된다)
  - **복구 리허설** — S3 → 맥 `ledger_restore_test` 에 복원, 19테이블 `COUNT(*)` 가 운영과 일치. 대조 쿼리는 아래. 두 번 걸렸다 — 트러블슈팅 18(예시의 `…`), 19(Safari 자동 압축 해제)
    ```
    SET SESSION group_concat_max_len = 100000;
    SELECT GROUP_CONCAT(CONCAT('SELECT ''', table_name, ''' AS t, COUNT(*) AS c FROM `', table_name, '`') ORDER BY table_name SEPARATOR ' UNION ALL ')
      INTO @q FROM information_schema.tables WHERE table_schema = DATABASE();
    PREPARE s FROM @q; EXECUTE s;
    ```
  - 그대로 둔 것 — `-mtime +7` 은 7~8개를 남긴다(실행 시각이 몇 초씩 흔들려 경계가 갈린다). 정확히 맞추려면 개수 기준으로 바꿔야 해서 얻는 게 없다
  - 실패 알림은 아직 없다(journal 뿐) — 5-6 UptimeRobot heartbeat 로
- **남은 것** — UptimeRobot(`/actuator/health` + 인증서 만료 + **백업 heartbeat**) / SES 샌드박스 해제 **신청**(승인 대기가 있어 먼저) / URL 공개 전 운영 `test@test.com` 정리 / EC2 `ledger.bak-certbot` 삭제(며칠 뒤)

### 4단계 가이드 — 게이트 (qa 146케이스)

- 구조 — `build` → `qa`(게이트) → `deploy`. deploy 의 `needs:` 에 qa 를 넣으면 실패 시 배포가 막힌다.
  QA 는 **GitHub Actions 러너(일회용) 안에서** 돈다. EC2·운영 DB 는 건드리지 않는다 (`reset-data.sql` 은 데이터를 지우므로 운영에서 돌리면 안 됨)
- `_lib.sh` 고칠 것 (위임, 기본값은 지금 로컬 동작 그대로) — 2026-09-17 실측
  - `ask_reset` 의 `read ANS` — 환경변수(예: `QA_RESET=y`)가 있으면 묻지 않고 진행
  - `mysql -u root -p` — 호스트·계정·비번을 환경변수로. 러너의 MySQL 은 서비스 컨테이너라 소켓이 아닌 `-h 127.0.0.1`
  - **`summary()` 가 FAIL 이 있어도 종료코드 0** — `FAIL > 0` 이면 `exit 1`. 이게 없으면 게이트가 항상 통과한다
  - `BASE` 도 환경변수로 덮을 수 있게
- 워크플로 `qa` 잡 — MySQL 서비스 컨테이너(지금 build 잡에 있는 것과 같은 설정) → jar 백그라운드 기동 → 헬스 대기 루프 →
  계정 시드(가입 API curl 2회: `test@test.com`, `rollback@test.com`, 비번은 CI 전용 `QA_PASSWORD`) → `qa/test-*.sh` 4개
- **근거 보강 (2026-09-18)** — 2-5 의 대소문자 사고는 리눅스에서 쿼리를 한 번이라도 날렸으면 잡혔다. CI 는 리눅스지만 `@Test` 가 `contextLoads` 하나뿐이라 통과했고, 146케이스는 맥에서만 돌았다. 게이트가 막는 게 정확히 이런 것 (ADR-045)
- 검증 — 일부러 서비스 검증 한 줄을 깨서 push → qa 빨간불 + **deploy 가 안 도는 것** 확인 → 되돌림
- 끝나면 — CLAUDE.md 협업 방식·qa-체크리스트에 "push 하면 CI 가 146케이스를 돌린다" 반영 ✅

#### 진행 기록 (4단계)

- **4-1 `_lib.sh` 비대화 모드 ✅ (2026-09-20, `06cf08f`, 위임)** — 환경변수를 안 주면 로컬 동작 그대로
  - `summary()` 끝에 `[ "$FAIL" -eq 0 ] || exit 1` — **게이트 전체가 이 종료코드 하나에 매달려 있다.** `summary` → 스크립트 → 스텝 → 잡 → `needs` 순으로 실패가 전달된다
  - `ask_reset()` — `QA_RESET`(y/n)이 있으면 `read` 를 건너뛴다 / `BASE` 도 환경변수로
  - `qa_mysql()` 신설 — DB 접속을 한 곳으로. `QA_DB_HOST`(러너의 MySQL 은 서비스 컨테이너라 소켓이 없다 → `-h 127.0.0.1`) · `QA_DB_USER` · `QA_DB_PASSWORD`.
    비번은 명령줄 인자가 아니라 `MYSQL_PWD` 로 넘긴다(인자는 프로세스 목록에 보이고 mysql 이 경고를 찍는다). 없으면 기존대로 `-u root -p` 프롬프트
  - `reset-all.sh` 가 `_lib.sh` 를 source — 서버 주소·DB 접속이 두 군데면 CI 값도 두 군데 넣어야 한다(ETC). `signup()` 은 실패 시 응답 본문을 같이 출력
  - 감수 — `test-category-transfer.sh` 는 `summary` 뒤에 "수동 확인" 안내가 있는데 FAIL 이 있으면 안 찍힌다(실패한 판엔 의미 없는 안내)
- **4-2 맥 회귀 ✅** — 기존 방식 그대로 21 + 35 + 38 + 52 = **146 / FAIL 0**, 종료코드 0. `(y/N)` 질문·비번 프롬프트 그대로.
  도중에 `reset-all.sh` 가입 400 — 스크립트가 아니라 zsh 따옴표 함정이었다 (트러블슈팅 15)
- **4-3 `qa` 잡 ✅ (`c350a27`, CI #52)** — `mysql:8.4` 서비스 컨테이너 → build 의 jar artifact 받기(**검사한 그 jar 가 배포되는 jar**, 다시 빌드 안 함) → `nohup java -jar` + 헬스 200 대기(3초×30) → `bash qa/reset-all.sh` 로 계정 시드 → `qa/test-*.sh` 4개 → 실패 시 `app.log` 200줄
  - 계정 시드는 yml 에 curl 을 쓰지 않고 **`reset-all.sh` 재사용** — 계정 목록이 저장소에 한 곳만 있다. 빈 DB 에 TRUNCATE 가 한 번 도는 건 무해
  - **`TZ: Asia/Seoul` 을 잡과 MySQL 컨테이너 둘 다에** — 러너는 UTC, 앱은 JVM KST 고정. `test-transaction.sh` 가 `date` 로 만드는 "내일"이 **한국 시각 00~09시엔 앱 기준 "오늘"** 이 되어 "미래 날짜 → 400"(16번)이 201 로 깨진다.
    새벽에 push 할 때만 빨개지는 테스트가 될 뻔했다. ADR-026 의 3계층을 CI 에도 그대로 적용한 것. 로그의 `오늘=` 로 확인
  - `sh` 가 아니라 `bash` 로 실행 — 우분투의 `sh` 는 dash 라 `echo` 가 백슬래시를 해석한다(JSON 본문 검사가 달라질 수 있다)
  - 스크립트 하나가 실패해도 **나머지를 끝까지 돌리고** 끝에 실패한 스크립트 이름을 출력(`::group::` 을 열지 않아도 보이게)
  - `QA_PASSWORD` 는 CI 전용 일회용 값. `qa/test-*.sh` 글롭이라 **새 스크립트는 파일만 추가하면 게이트에 들어간다**
  - **초록불을 믿지 않고 원본 로그로 확인** — `healthy after 5 tries` / 가입 201 두 건 / `PASS 21·35·38·52` / `오늘=2026-09-20`(KST). 막으려던 사고가 "항상 통과하는 게이트"라서다.
    리눅스 + MySQL 8.4 에서 146케이스가 **처음으로** 돌았다 (ADR-045 같은 사고를 배포 전에 잡는 자리)
- **4-4 `needs: [build, qa]` ✅ (`8d91688`, CI #53)** — qa 가 초록인 걸 한 번 본 **뒤에** 묶었다. 같이 바꿨으면 qa 잡 자체의 문제(러너의 mysql 클라이언트 등)로 배포까지 막혀 원인이 섞인다.
  #53 은 build → qa → deploy **차례로** 전부 초록, 약 3분(게이트 전 약 2분 — ADR-042 의 감수)
- **4-5 일부러 깨기 ✅ (CI #54 → #55)** — 4-4 와 **push 를 나눴다**(같이 올리면 deploy 가 안 돈 게 게이트 덕인지 `needs` 오타인지 구분이 안 된다)
  - 깬 줄 — `PersonalTransactionRequestDto.transAmount` 의 `@Min(1)` 주석 처리(`1321c03`). 맥에서 QA 를 돌리지 않고 바로 push
  - **#54 — build ✅ / qa ❌ / deploy ⏭ skipped.** `test-transaction.sh` 12번(금액 0)·13번(음수)만 `expected 400 got 500`, `PASS 50 / FAIL 2`. 나머지 3개 스크립트는 끝까지 돌아 전부 통과
  - build 가 초록인 게 요점 — 컴파일 에러가 아니고 `@Test` 는 `contextLoads` 하나. **빌드가 못 보는 걸 게이트가 본다**
  - 400 이 아니라 **500** 인 이유 — `@Min` 이 빠지자 값이 DB 까지 갔고 `CHECK` 제약이 막았다. 금액 삼중 검증(화면·서버·DB)의 마지막 방어선이 실제로 일했고, 응답은 고정 문구라 제약 이름이 새지 않았다
  - 되돌림 — `git revert HEAD`(`3a44171`). `reset` + force push 는 `protect-main` 이 막는다(3단계 보호가 여기서 일했다). **#55 — 146 / FAIL 0, deploy `healthy after 4 tries`**
  - `8d91688` 과 `3a44171` 의 트리가 같다(`git diff` 0줄) — 깨기 전 상태로 정확히 돌아왔다
- 참고 — Actions 주석: `ubuntu-latest` 가 **2026-10-19 부터 Ubuntu 26** 으로 바뀐다. 운영(26.04)과 가까워지는 쪽. 그날 이후 첫 push 에서 qa 가 깨지면 러너 이미지부터 의심

---

## Sprint 1 — 남은 순서

~~거래 CRUD(8단계 화면)~~ ✅ → ~~통계 화면(위임)~~ ✅ (2026-09-17) → ~~거래 화면 합산 교체~~ ✅ (2026-09-17, ADR-040) → **1차 배포(CD·EC2·HTTPS, SES 샌드박스 해제 신청)** → 로그인 잠금 → 이메일 가입 인증 + 비번 재설정(메일, 같은 SES·`AUTH_VERIFICATIONS`) → Sprint 2

배포를 앞당긴 이유는 기획서 스프린트 표 아래 참고.

---

## Sprint 1 종료 시 반드시 다시 꺼낼 것

- [ ] 리팩토링 판단 기준 — "다음 기능이 이 코드를 복제하게 되는가?" 그렇다면 그 기능 **직전**에, 아니면 여기 적고 넘어감 (ADR-038 정리 때 세운 기준)
- [ ] 카테고리 이름 중복 차단 (ADR-018). **입구 정규화(앞뒤 공백 제거)는 2026-09-17 선반영** — 남은 건 중복 판정 자체 (ADR-044)
- [ ] `USER_ID` 공개 핸들 — 유저 검색 기능 때 가입 폼에 추가 (ADR-013)
- [ ] 운영 프로파일 분리 — 배포 직전 → **진행 중** (위 "1차 배포" 1~2단계, ADR-042)
- [ ] 휴면 전환 스케줄러 — Sprint 2 스케줄러 때 (ADR-029)
- [x] `@Max` 메시지 문구 "1조원 미만" → "이하" (이미 반영돼 있었음, 2026-09-16 확인)
- [ ] 소셜 로그인 — 배포 후, 네이버 → 구글 (개발자 콘솔에 배포 URL 등록 필요). **카카오 로그인은 안 함** (2026-09-18, 카카오맵은 별개). 기획서 반영 완료
- [ ] 운영 3종 — systemd 자동 재시작(✅ 2-5) / DB 백업 → S3(✅ 5-5, ADR-050) / UptimeRobot. 이중화 대신 이걸로 (ADR-002 보완, 배포 직전)
- [x] **배포 시 타임존 3계층 확인** — OS·MySQL·JVM 모두 KST 인지 (ADR-026 주의 참고). 로컬에선 안 드러남.
      OS·MySQL 은 2-2(2026-09-17), **JVM 은 2-5 에서 확인 완료(2026-09-18)** — journald(OS)와 Spring 로그(JVM) 시각이 같은 `+09:00`, 서버 `date` 날짜로 넣은 거래가 201
- [ ] **뷰 대공사** — 메시지 표시를 한 곳으로 모은다. 지금은 세 갈래: `handleConflict()` alert /
      `removeTransaction()` alert / submit 의 `resultMessage`(모달 안). 투박해도 깔끔·단순하게,
      모달 결과 메시지 UI 를 만들고 셋을 함수 하나로 합칠 것. 달력 뷰·PWA 와 같이 묶어도 됨
      + 수정 모달의 지출/수입 탭이 "종류 선택"임을 드러내기 (필터처럼 보임. 동작은 의도된 것 — 타입은 카테고리의 그림자, ADR-008)
      + 조회 실패(500·네트워크) 시 화면이 조용히 멈춤 — `loadTransactions`·`loadStatistics` 에 catch 없음. 메시지 UI 만들 때 같이
      + 통계 봉투를 펼치면 대분류 직접 거래 줄이 대분류와 같은 이름으로 보임 — 헷갈리면 "(직접)" 표시 (2026-09-17 화면 QA)
      + `login.html`·`signup.html` 의 빈 `<span id="…Error" class="error-message">` 5개 + `.error-message` CSS — 어떤 JS 도 안 쓴다(오류는 전부 `resultMessage`). 다만 `.form-container` 의 `gap: 12px` 때문에 **빈 span 이 "입력칸 → 다음 라벨" 간격 24px 를 만들고 있어** 그냥 지우면 폼이 붙는다. 메시지 UI 를 정할 때 쓰든지(필드별 오류), 지우고 간격을 CSS 로 주든지 (2026-09-21 미사용 코드 스캔)
- [ ] 달력 뷰 — 목록/달력 토글. 백엔드 변경 없음(같은 월 조회 API를 다르게 그리는 것)이라 급하지 않음. 개인 통계 끝나고 판단
- [ ] 패스키(WebAuthn) — 지문·FaceID로 비밀번호 대체. 웹·PWA 공통 표준. Sprint 5 이후
- [x] **거래 화면 합산 → 서버 값으로 교체 (ADR-038 미룬 것)** — 2026-09-17 완료. `/api/statistics` 값 표시, 세 요청 `Promise.all` (ADR-040).
      같은 날 서버/뷰 전체 스캔 — 서버 위반 0, 프론트 위반은 이 1건. 회색 판정은 ADR-040. signup 입력 보조(`maxlength`) 추가
      (이전 기록) `transaction.js render()` 가 수입/지출/잔액을 직접 합산 중.
      통계 API 가 `totalIncome / totalExpense / balance` 를 내려주게 되면 그 값을 읽어 표시만 하도록 교체.
      타이밍: 통계 조립(최상위 DTO) 끝난 직후. 그 전엔 건드리지 않음 (통계 API 없이 바꾸면 요청 하나가 더 생김)
- [ ] 거래 목록 "N건" → 페이징 붙일 때 서버 totalCount 로 (지금은 보이는 줄 수라 뷰에 둠, ADR-040)
- [ ] **바꾸기 쉬운 구조 — 시점별 (ADR-041, 2026-09-17 스캔).** 지금 고치지 않는다. 해당 기능 첫 단계에서 먼저 꺼낼 것
      + **배포 직전** — 운영 프로파일: `application.yml` 의 `localhost` DB URL · 매퍼 `DEBUG` 로그 (기존 "운영 프로파일 분리"와 같은 작업)
      + ~~**CD 만들 때** — `qa/test-*.sh`(146케이스)를 배포 전 게이트로 묶을지 결정. CI 는 `@Test` 1개뿐이라 회귀가 수동~~ → **도입 결정** (2026-09-17, ADR-042). 구현은 "1차 배포" 4단계
      + **로그인 잠금 직전** — 예외를 의미별 부모 클래스로 묶어 핸들러 수 고정(현재 15개) / `/api/users/me` 응답을 `LoginResponseDto` 에서 분리
      + **Sprint 2, 거래에 필드가 붙기 직전** — `PersonalTransactionService` 값 채우기·응답 변환 한 곳으로(create/update 2벌), 소유권 확인 private 메서드(7곳)
      + **통계 다음에 건드릴 때** — 비율 계산 3벌 → `percent(part, whole)`, 추이 개월 수 `5`/`6` 리터럴 한 상수로
      + **달력 뷰 직전** — 월 이동(year/month/changeMonth) util 추출. `category.js` 의 `editingId`(hidden input)도 `transaction.js` 처럼 JS 변수로
      + **Sprint 1 전체 리뷰** — `PersonalTransactionService` 힌트 블록 주석 100줄 → "왜"만 남기기 (체크리스트 8번)
- [ ] **Sprint 2, Ollama 설치 직전** — 인스턴스 t4g.small → t4g.medium (중지 → 유형 변경 → 시작, ADR-002 보완). 그 전에도 `free -h` 로 메모리가 빠듯하면 올린다
- [x] **세 환경 MySQL 버전 정렬 — 4단계 게이트 때** (2026-09-20 완료 — `ci.yml` build·qa 둘 다 `mysql:8.4`. 맥은 그대로) (2026-09-17 실측: 맥 **26.7** / CI **8.0** / EC2 **8.4.11**). 셋이 전부 달랐다
      + 결정: CI 이미지를 `mysql:8.4` 로 올린다 — 게이트는 **운영과 같은 버전**에서 통과해야 의미가 있다. 8.0 은 2026-04 지원 종료라 어차피 못 쓴다
      + 맥 26.7 은 그대로 둔다 (개발 편의). 맞춘 건 collation 이고 버전까지 같게 하지는 않는다 — 다르면 드러나는 곳은 게이트이므로 CI 만 맞추면 된다
      + Flyway 가 `Using MySQL 26.7 which is newer than the version Flyway has been verified with` 경고를 내는 것도 이 때문. 로컬 전용 경고라 무시
- [x] (2026-09-20 완료, `06cf08f` — 고친 당일 트러블슈팅 15 의 원인을 본문 한 줄로 바로 좁혔다) `qa/reset-all.sh` 의 `signup()` 이 `-o /dev/null` 로 **응답 본문을 버려서 실패 원인이 안 보인다** (2026-09-17, `QA_PASSWORD` 가 비번 규칙에 안 맞아 400 이 났을 때 원인 추적에 시간을 씀). 실패했을 때만 본문을 찍도록 — 4단계 `_lib.sh` 비대화 모드 작업에 같이 (위임)
- [x] **기획서 재점검 — md 완료 (2026-09-18), 노션은 md 기준으로 대조 예정.** 반영: Redis 삭제(서버 2대부터) / 예산표 t4g.small 30GB 기준 / Swagger Sprint 2 / REST 표현 / 통계 용어 / 소셜 로그인 네이버·구글(카카오 삭제) / 등록 진입점 캡처·직접 2개(토글은 백로그) / 규제 근거(마이데이터 허가제·오픈뱅킹 카드 없음) / 보안 4건(Access 메모리·CSRF·가입 이메일 인증·Nginx 헤더) / 잠금 계정+IP 주석
      (원래 항목) 1차 배포 완료 직후 ① — 기획서·노션 재점검 (2026-09-18 결정). 배포하며 어긋난 걸 한 번에 맞춘다 — t3.medium → t4g.small(예산 5.7만 → 3.3만) / Redis "컨테이너" → 지금은 앱 메모리·MySQL, 서버 2대부터 Redis / Sprint 1 의 소셜 로그인·비번 재설정 뒤로 / Sprint 5 의 배포 앞당김 / EBS 20 → 30GB. 저장소 `기획서.md` 와 노션 원본 둘 다
- [ ] **1차 배포 완료 직후 ② — 배포·인프라 총정리 문서** (2026-09-18 요청). "어떤 식으로 이루어졌는가"를 한 장에 — 요청 흐름(사용자 → Nginx → `localhost:8080` 앱 → `localhost:3306` MySQL) / 계정 표(`ubuntu` 사람 · `ledger` 앱 · `ledger_app` DB · CD 배포 계정) / 디렉토리·소유권 표(`/opt/ledger` · `/etc/ledger/ledger.env` · 유닛 파일) / 환경변수 표(세 환경) / 배포 절차(수동 → CD) / 단계별 실측 이력. `aws-인프라.md` 를 확장할지 `docs/배포.md` 를 새로 둘지는 그때 결정
      + **꼭 짚고 넘어갈 것 (2026-09-19 추가)** — 진도를 빼느라 "왜"를 묻지 않고 지나간 것들. 총정리 문서를 쓸 때 **항목마다 한 문단씩 답을 적는다.**
        면접 답변이 목적이 아니라, 답을 못 쓰면 그 부분을 내가 운영할 수 없다는 뜻이라서다. 진행하다 새로 생기면 그때그때 이 목록에 한 줄 추가한다
        - [ ] 탄력적 IP 가 없으면 무엇이 깨지는가 (재시작마다 IP 가 바뀌면 누가 곤란한가)
        - [ ] 보안 그룹은 어디에 있는 방화벽인가 — 서버 안의 `ufw` 와 무엇이 다른가. "내 IP 만" 이 실제로 막는 것
        - [ ] 공개키 인증의 원리 — `authorized_keys` 에 공개키를 넣으면 왜 비밀번호 없이 들어가지는가. pem 과 `ledger-deploy` 키의 차이
        - [ ] `systemd` 유닛 파일을 한 줄씩 — `Type=simple` 이 `active` 의 의미를 어떻게 바꾸는가(3-3 실측), `Restart=always` 와 `enable` 이 각각 무엇을 닫는가
        - [ ] JRE 만 깔고 JDK 를 안 깐 판단 — "이 컴퓨터가 하는 일이 빌드인가 실행인가"
        - [ ] Flyway 체크섬이 무엇이길래 V1 을 고치면 DB 를 지워야 했는가 (ADR-045)
        - [ ] Spring 프로파일의 merge 규칙 — `application-local.yml` 이 "다른 값만" 가지는 이유
        - [ ] 리버스 프록시(Nginx)가 하는 일 — 8080 을 닫고 80·443 만 여는 게 무엇을 막는가 (5단계에서 실제로 세우며)
        - [ ] OIDC — 액세스 키 대신 쓰는 이유, GitHub 이 발급한 토큰을 AWS 가 믿는 근거 (3단계 B안)
        - [ ] EC2 한 대에 앱·DB 를 같이 올린 대가 (ADR-002) — 무엇을 포기했고 언제 나눠야 하는가
        - [ ] 게이트는 어떻게 배포를 막는가 — `summary()` 의 종료코드 → 스텝 → 잡 → `needs` 사슬. build 는 초록인데 qa 가 잡은 것(CI #54)이 무엇을 뜻하는가. 서비스 컨테이너는 어디서 돌고 왜 `-h 127.0.0.1` 인가 (4단계)
        - [ ] **실무는 이 문제를 어떻게 푸는가** (2026-09-19 추가) — 우리가 고른 것과 나란히 적으면 "왜 이렇게 했나"의 답이 된다
          - [ ] 배포 중 SSH 포트 — 열었다 닫는 건 우회책. 실무 정답은 **열 필요가 없게** (SSM Session Manager / 서버가 당겨가는 pull 방식(CodeDeploy·Argo CD) / VPC 안의 self-hosted 러너). 우리도 SSM 으로 갈 수 있다(우분투에 에이전트 기본 탑재 + 인스턴스 역할) — 5단계 이후 후보
          - [ ] 롤백 — 고치지 않고 **되돌린다**. 이전 아티팩트 재배포 / 블루·그린·카나리(트래픽만 되돌리면 초 단위) / `kubectl rollout undo` / 지표 기반 자동 롤백.
                **DB 마이그레이션은 롤백이 어렵다 → forward-only + 이전 버전 코드와도 호환되게**(컬럼 삭제는 "안 쓰게 만들기 → 다음 배포에서 삭제" 두 단계). V2 부터 적용될 원칙
          - [ ] 무중단 배포 — 조건은 "새 버전이 뜬 걸 확인한 뒤 트래픽을 옮긴다". LB + 인스턴스 2대 롤링 / 블루·그린 / K8s readiness probe.
                **한 대에서도 축소판이 된다** — 새 jar 를 8081 로 띄우고 헬스 통과 후 Nginx upstream 교체 + reload(5단계에 Nginx 가 들어온 뒤). 지금 막는 건 구조가 아니라 메모리(앱 1개 약 300Mi / available 608Mi)
          - [ ] main 브랜치 보호 — 실무는 main 직접 push 금지, 전부 PR(리뷰 1명 + CI 통과 필수 + force push·삭제 금지 + CODEOWNERS). 1인 프로젝트에서 실효가 있는 건 **force push·삭제 금지와 상태 체크 필수**(저장소 소유자는 우회 가능 — 막는 건 "실수")
          - [ ] 테스트 — 게이트가 푸는 건 "사람이 기억해서 돌리는 문제"지 테스트 품질이 아니다. 실무는 빠른 단위 테스트가 바닥에 많은 **피라미드**. curl 통합 테스트는 느리고 실패 원인이 거칠다 → Sprint 4 JUnit 전환 때 qa 잡 내용만 교체
          - [ ] **CI/CD 를 어떤 프로젝트에 넣는가** (2026-09-20) — 갈리는 기준은 "자사냐 외주냐"가 아니라 **그 코드가 계속 살아 있는가**다. 배포 빈도 × 손대는 사람 수 × 틀렸을 때 비용이 크면 넣는다.
                자사 서비스는 수명이 길고 배포가 잦고 사람이 들고 나서 거의 항상 넣는다 — 배포 절차가 누군가의 머릿속에만 있으면 그 사람이 자리를 비울 때 배포가 멈추고, 규모가 커지면 "회귀를 사람이 기억해서 돌린다"가 가장 먼저 무너진다(4단계를 넣은 이유 그대로).
                SI·외주가 전통적으로 약했던 건 기술이 아니라 **계약과 환경** 때문이다 — 납품·검수로 끝나 "지속적"의 대상 기간이 짧고, 서버가 고객사 소유라 개발사에 배포 권한이 없고, 금융·공공은 망분리·변경관리 절차 탓에 외부 SaaS 러너가 사내망에 닿지 못한다. 그래서 CI(빌드·정적분석·테스트)만 넣거나 Jenkins·GitLab CI 를 내부망에 세우는 형태가 많다(규제는 완화 흐름이라 현재 기준은 최신 자료 확인).
                **Continuous Delivery ≠ Deployment** — 전자는 "언제든 배포 가능한 상태까지"만 자동이고 운영 반영은 사람이 누른다(규제 산업·대형 조직의 기본형. 개발계 → 검증계 → 운영계 + 승인 게이트). 후자는 운영까지 자동이고 **우리가 이쪽**이다(main push = 운영 반영).
                쓸 문장은 "CI/CD 해봤다"가 아니라 **"우리는 어디까지 자동이고 무엇을 사람이 누르는가, 왜"** 다
                **안 넣는 게 맞는 경우** — 한 번 납품하고 끝나는 결과물, 버려질 프로토타입, 분기 1회 배포하는 내부 도구, 수동 반출이 규정인 환경. 파이프라인도 유지보수 대상이라 배포가 드물면 파이프라인이 먼저 썩는다
          - [ ] 액션 SHA 고정이 막는 것 — `@v4` 는 버전이 아니라 **옮길 수 있는 이름표**. 액션 저자 계정이 털리면 다음 실행부터 내 Secrets 가 샌다(2025년 실제 사고). Dependabot 을 켜면 고정의 단점(수동 업데이트)이 사라진다
          - [ ] 호스트 키 검증 — `accept-new` 는 러너가 매번 새 머신이라 사실상 검증 없음. `ssh-keyscan` 결과를 `EC2_HOST_KEY` Secret 에 넣고 `known_hosts` 고정 (3-6 성공 직후)
- [ ] **배포 URL 공개 전 — 2-5 확인용 계정 정리** (2026-09-18). `test@test.com` 을 운영 DB 에 만들어 가입·로그인·거래를 확인했다. 비번이 QA 용 단순 값이라, 5단계에서 주소를 공개하기 전에 지우거나 바꾼다
- [ ] **Redis 도입 직전 — 정말 필요한가부터** (2026-09-18). 서버 한 대면 카운터를 공유할 상대가 없고 2GB 에 상주 프로세스 하나가 더 는다. 호출 제한 카운터는 앱 메모리, Refresh Token 은 MySQL 테이블(기획서의 정리 스케줄러 방식). 넣기로 하면 apt 패키지 vs 도커 결정 — 앱은 systemd 라 도커를 깔면 "왜 앱은 아니냐"가 생긴다
- [ ] **Sprint 2, 이미지가 서버를 거치게 되면** — 앱이 쓰는 파일 자리 `/var/lib/ledger`(`ledger` 소유)를 따로. `/opt/ledger` 는 앱이 못 쓰는 자리로 유지
      + **먼저 물을 것 — 이미지가 서버를 거쳐야 하는가** (2026-09-20). 기준은 "누가 만든 파일인가"
        - 우리가 만든 고정 이미지(로고·소개 화면) → 저장소 `static/` 에 두고 jar 에 포함. 코드와 같이 버전 관리되고 배포하면 같이 올라간다. S3 아님
        - 사용자가 올리는 파일(영수증·프로필) → **S3**. 서버 디스크에 두면 서버를 새로 만들 때 사라지고, jar 배포와 수명이 다르고, 용량이 계속 는다
      + 실무 패턴 — 브라우저가 **프리사인드 URL 로 S3 에 직접 업로드**, 서버는 경로만 DB 에. 버킷은 비공개, 볼 때도 만료 시간이 있는 URL 을 서버가 발급(영수증은 개인정보).
        이 방식이면 이미지가 서버를 안 거치므로 **위 `/var/lib/ledger` 항목 자체가 필요 없어질 수 있다.** 다만 OCR(Clova)에 넘기려면 서버가 이미지를 읽어야 한다 — "S3 에서 읽어 넘기기 vs 서버가 받아서 넘기고 S3 에 저장" 을 Sprint 2 첫 단계에서 결정
      + 메인 화면의 예시 화면은 이미지 파일이 아니라 실제 화면과 같은 CSS 클래스로 그렸다 — 다크모드·디자인 변경을 그대로 따라오고 관리할 자산이 없다. 홈 스타일은 `common.css` 의 `.home` 블록(50줄)에 둠. **CSS 를 파일로 나누는 시점 — `common.css` 가 한 화면에서 훑기 어려워질 때(대략 500줄) 또는 한 화면 전용 블록이 100줄을 넘을 때.** 지금은 256줄 / 9.6KB 라 나누면 요청만 하나 는다
- [ ] 서비스 변수명 통일 — `newPersonalTransaction` / `transaction` 혼재 여부 확인
- [ ] 패키지 도메인별 분리 — Sprint 4 직전 (ADR-037)
- [ ] 들여쓰기 통일 — 자바 파일 탭 29 / 스페이스 16 혼재. Sprint 1 전체 리뷰 때 포매터(스페이스 4 or 탭) 하나로 정하고 한 커밋에 일괄. 기능 커밋에 섞지 않는다
      + static 도 — 탭 들여쓰기: `user/login·signup` js·html 4파일 / 파일 끝 개행 없음: `error/404·500`, `main.js`, `authGuard.js`, `buttonUtil.js`, `user/` 4파일 (2026-09-17 실측)
- [x] **`month` 범위 검증 (500 → 400)** — A안 채택 (2026-09-16). `@RequestParam @Min(1) @Max(12) int month` + `HandlerMethodValidationException` 400 핸들러.
      `/api/statistics`, `/api/transactions` 둘 다 적용. B안(`DateTimeException` 핸들러)은 서버 내부 날짜 버그까지 "사용자 잘못"으로 덮어서 탈락 (ADR-039)
- [ ] **멱등성(중복 요청 서버 방어) — Sprint 4 "깊게 팔 영역" 후보** (2026-09-17 결정: 로드맵에 적는다)
      + 지금 — 화면에서만 막는다. `withButtonLock`(`buttonUtil.js`)이 submit 버튼을 잠금: 거래·카테고리·가입·로그인 4곳
      + 뚫리는 경로 — curl·스크립트 직접 호출 / 응답을 못 받은 클라이언트의 재시도(모바일 네트워크, PWA) / 더블 탭이 잠금보다 빠른 경우. 결과는 **같은 거래가 두 줄** = 돈 기록이 틀어짐 (낙관적 락은 "수정 충돌"만 잡고 "생성 중복"은 못 잡는다, ADR-006)
      + 왜 Sprint 4 — 공동 가계부에선 중복 지출이 곧 정산 금액 오류라 피해가 남에게 간다. 그 전엔 본인 화면에서 보이고 지우면 됨
      + 그때 정할 것 — 멱등 키(클라이언트가 요청마다 UUID, 헤더 `Idempotency-Key`) 저장 위치(DB 유니크 vs Redis TTL) / 같은 키 재요청 시 응답(첫 응답 재생 vs 409) / 어느 API 까지(POST 생성만?)
      + qa-체크리스트 "같은 요청을 두 번 보내면?" 축의 서버 쪽 답이 이것
- [ ] curl QA → JUnit 단위 테스트 전환 — Sprint 4 "깊게 팔 영역" (docs/qa-체크리스트.md 한계 참고)

---

## 현재 파일 구조 (static)

```
views/
  main.html
  user/         login.html, signup.html
  personal/     personal-main.html, category.html, statistics.html
js/
  main.js
  util/         apiClient.js, authGuard.js, buttonUtil.js, format.js, theme.js
  user/
  personal/     category.js, transaction.js, statistics.js
css/
  common.css
error/
  404.html, 500.html
```

하위 네비(`.sub-nav`)로 가계부 ↔ 카테고리 ↔ 통계 이동.
