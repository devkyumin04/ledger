<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kyumin.dotoree.mapper.PersonalStatisticsMapper">

	<!--
		통계 쿼리를 쓸 때 꼭 지킬 것

		1. WHERE 에 컬럼을 가공하지 않는다
		   ✗ WHERE YEAR(trans_date) = 2026      → Table scan (전체 읽음)
		   ✓ WHERE trans_date >= #{startDate}   → Index range scan (구간만 읽음)
		   EXPLAIN 으로 확인한 결과: 297행 vs 3858행 (13배)
		   SELECT / GROUP BY 에서 함수를 쓰는 건 괜찮다. 이미 걸러낸 뒤라서.

		2. XML 이라 '<' 를 그대로 못 쓴다 → &lt; 또는 <![CDATA[ ... ]]>

		3. 소프트 딜리트 — 거래(t.use_yn) 만 확인한다. 카테고리는 삭제 시 거래가 미분류로 이관되므로(ADR-023)
		   살아있는 거래가 죽은 카테고리를 가리키는 경우가 없다

		4. 테이블 별칭(t, c) 명시 — 양쪽에 category_num 이 있어 모호해진다

		5. 사용자 격리 — WHERE 에 user_num 필수
	-->

	<select id="findCategoryStats" resultType="com.kyumin.dotoree.dto.CategoryStatRowDto">
		SELECT 
			c.category_num, 
			c.category_name, 
			c.category_emoji, 
			c.parent_category_num,
			p.category_name  AS parent_category_name,
			p.category_emoji AS parent_category_emoji, 
			SUM(t.trans_amount) AS amount,
			c.category_type
		FROM personal_categories c
		JOIN personal_transactions t
		ON t.category_num = c.category_num
		LEFT JOIN personal_categories p
		ON c.parent_category_num = p.category_num
		WHERE t.user_num = #{userNum}
		AND t.use_yn = 'Y'
	<![CDATA[
		AND t.trans_date >= #{startDate}
		AND t.trans_date < #{endDate}
	]]>
		GROUP BY c.category_num
		ORDER BY amount DESC
	</select>

	<select id="findMonthlyStats" resultType="com.kyumin.dotoree.dto.MonthlyStatRowDto">
		SELECT DATE_FORMAT(trans_date, '%Y-%m') AS yearMonth,
				SUM(CASE WHEN trans_type = 'I' THEN trans_amount ELSE 0 END) AS totalIncome,
				SUM(CASE WHEN trans_type = 'E' THEN trans_amount ELSE 0 END) AS totalExpense
		FROM personal_transactions
		WHERE user_num = #{userNum}
		AND use_yn = 'Y'
	<![CDATA[
		AND trans_date >= #{startDate}
		AND trans_date < #{endDate}
	]]>
		GROUP BY yearMonth
		ORDER BY yearMonth
	</select>

</mapper>
