# Ledger V3.0

AI 영수증 인식 및 지도 기반 개인·공동 가계부. 24시간 상시 운영 실서비스를 목표로 개발 중.

상세 문서는 `docs/` 참조:

| 문서 | 내용 |
|---|---|
| [기획서](docs/기획서.md) | 기능 명세, AI 파이프라인, 보안 정책, 비즈니스 룰, 스프린트 계획 |
| [테이블 설계](docs/테이블설계.md) | 18개 테이블 물리 설계 (컬럼·타입·제약·인덱스) |
| [설계 결정 기록](docs/decisions.md) | 왜 그렇게 결정했는지 (ADR) |
| [트러블슈팅](docs/트러블슈팅.md) | 겪은 문제와 해결 과정 |
| [진행 상황](docs/진행상황.md) | 스프린트별 완료·예정 항목 |
| [QA 체크리스트](docs/qa-체크리스트.md) | 케이스를 뽑는 5가지 축. 기능 만들 때마다 대입 |
| [설계 체크리스트](docs/설계-체크리스트.md) | 구조가 규칙대로인가. QA 통과 직후 커밋 전에 대입 |
| [AWS 인프라](docs/aws-인프라.md) | AWS 에 지금 무엇이 어떤 값으로 있는지, 왜 그렇게 골랐는지, 자주 하는 조작(접속·유형 변경·요금). 값(IP·키)은 없음 |
| 로컬환경.md (git 제외) | 테스트 계정·QA 실행법 등 커밋하면 안 되는 **로컬 메모**. `.gitignore` 등록됨. **비밀번호·키 자체는 여기가 아니라 저장소 밖 개인 보관처에** |

---

## 기술 스택

- **백엔드** — Java 17, Spring Boot 4.0.8, MyBatis, MySQL, Flyway, Spring Security, JWT(jjwt), Lombok
- **프론트** — Vanilla JS(ES6+), HTML5/CSS3, Fetch API, PWA 예정
- **AI** — 네이버 Clova 일반 OCR + 로컬 LLM(Ollama) + 한국어 문장 임베딩
- **지도** — 카카오맵 API
- **인프라** — AWS EC2(ARM t4g · Ubuntu 26.04 LTS. 지금 t4g.small, Ollama 도입 때 t4g.medium) 단일 인스턴스에 Spring Boot + MySQL + 임베딩 모델 (Redis 없음 — 서버 2대부터), S3, Nginx + Let's Encrypt, GitHub Actions. 도메인 `dotoree.app`(Cloudflare, DNS only)

패키지 루트: `com.kyumin.ledger`

---

## 설계 감각 (규칙보다 위에 있는 기준)

두 원칙을 같이 쓴다. 부딪히면 ①이 이긴다 — ②는 ①을 위한 수단이다.

1. **바꿀 때 싸게 바뀌는가 (ETC, Easier To Change)** — 좋은 설계의 기준은 "잘 나뉘었나"가 아니라 "규칙 하나 바꿀 때 몇 군데를 고치나"
2. **서버는 진실, 뷰는 보여주기 (SSOT, Single Source of Truth)** — 돈·규칙은 계산하는 곳이 한 군데. 경계는 ADR-040

감각 세 가지:

- **규칙엔 이유와 예외 조건을 같이 적는다** — 이유가 해당 안 되는 곳에선 규칙을 푼다. 이유 없는 규칙이 교조가 된다 (그래서 ADR)
- **아픈 곳만 고친다** — "원칙 위반이라서"가 아니라 "이대로 두면 언제 무엇이 깨지는가"를 말할 수 있을 때만
- **리팩토링은 그 코드를 복제하게 될 기능 직전에** — 미리 완벽하게 나누지 않는다. 시점은 진행상황.md 로드맵에 "○○ 직전"으로 적는다 (ADR-041)

검사 방법은 설계-체크리스트 11번 (변경 시나리오 대입).

---

## 프로젝트 규칙

### 데이터 무결성

- **소프트 딜리트** — `USE_YN` CHAR(1), 기본 `'Y'`. 조회·수정 `WHERE`에 `use_yn = 'Y'` 필수
- **생성일시** — `CREATED_AT` DATETIME DEFAULT CURRENT_TIMESTAMP
- **낙관적 락** — `VERSION` INT. 수정·삭제 시 `SET`에 `version = version + 1`, `WHERE`에 `version = #{version}` 조건. affected rows가 0이면 충돌 → 409
- 금액 0/음수는 화면·서버 이중 검증. DB에도 CHECK 제약
- 조회 조건에 `USER_NUM` 강제 (사용자 간 데이터 격리)

### MyBatis

- `#{}` 바인딩만 사용. `${}` 금지 (SQL 인젝션)
- 매퍼 파라미터에는 개수와 무관하게 `@Param` 필수 (`-parameters` 옵션 의존 회피 + 규칙 단일화)
- `map-underscore-to-camel-case: true` 활성화됨
- 반환 타입: `WHERE` 조건 때문에 0행이 나올 수 있고 그걸 구분해야 하면 `int`, 아니면 `void`

### 마이그레이션

- DDL은 `V1__init_schema.sql` 하나로 통합 (아직 최초 적용 단계)
- V1 최초 적용 이후 스키마 변경은 V2, V3 순서로 새 파일 추가

### 계층 역할 (서버가 계산, 뷰는 표시)

- **판단·규칙은 서버** — 비율, 파생값, "할 수 있다/없다"(수정 가능 여부, 예약어 여부 등)는 서버가 계산해 응답 DTO에 실어 보냄. 프론트는 그 값을 읽어 표시·잠금만 한다
  - 예: `CategoryResponseDto.isDefault / hasChildren`. 프론트에 `'미분류'` 같은 예약어 리터럴을 두지 않는다
- **프론트에 남기는 것** — 받은 데이터만으로 답이 하나인 표시 로직(포맷, 부호·색상, 정렬·그룹핑, 월 자리올림)
- **화면 합계**는 통계 API 값을 쓴다 (화면마다 따로 합산하면 불일치). 거래 화면도 `/api/statistics` 값을 표시만 한다 (ADR-040)
- **나누는 것 자체가 목적이 아니다 (ADR-040)** — 목적은 "규칙·돈 계산을 바꿀 때 한 곳만 고치면 되는가". 서버로 옮길지는 질문 두 개로 정한다
  - 서버: 틀리면 돈·규칙이 틀어지는가? 다른 화면도 같은 값을 보여줘야 하는가?
  - 뷰: 이 화면에서만 의미 있는 모양인가? (막대 스케일, 펼침 여부, 선택지 좁히기, 입력 1차 방어, 보이는 줄 수)
  - 화면 모양 하나 때문에 응답 DTO 필드·요청을 늘리는 건 독. 애매하면 뷰에 두고 주석으로 이유를 남긴다
  - 서로 결과가 필요 없는 요청은 `Promise.all` 로 동시에 — 요청 수보다 기다리는 횟수가 체감 속도
- **응답 DTO 애노테이션** — 서버가 만들어 채우는 응답은 `@Getter @AllArgsConstructor`(불변). MyBatis/Jackson이 채우는 것(요청 DTO, 매퍼 row DTO)은 `@Getter @Setter @NoArgsConstructor`. 응답 DTO를 MyBatis resultType 으로 직접 쓰는 경우는 후자를 쓰고 이유를 주석으로 남긴다
- **컨트롤러 응답** — `ResponseEntity.status(HttpStatus.X).body(...)` 형태로 통일

### API

- REST — `GET` 조회 / `POST` 생성 / `PUT` 전체수정 / `DELETE` 삭제
- **에러 응답 분리** — API는 텍스트, 페이지는 HTML.
  `@RestControllerAdvice(annotations = RestController.class)`로 범위를 한정하고,
  `CustomErrorController`가 `/api`로 시작하면 `ResponseEntity`, 아니면 HTML forward
- 최종 `Exception` 핸들러는 `e.getMessage()`를 노출하지 않고 고정 문구 사용 (DB 스키마 노출 방지)

### 프론트

- 경로는 **절대경로** (`/views/...`, `/js/...`). 폴더 깊이가 달라지면 상대경로가 깨짐
- 에러 페이지는 forward라 URL이 원래 주소에 머무름 → CSS도 절대경로 필수
- `common.css` 통합 디자인 시스템. 색상은 CSS 변수 (`--bg`, `--text`, `--border`, `--surface`, `--accent`)
- 다크모드 — OS 설정(`prefers-color-scheme`) 기본값 + 토글 버튼, localStorage 저장
- UI/UX는 간결하게, 기능 우선
- **HTML 에 인라인 코드 금지 (CSP, ADR-049)** — `onclick=` 같은 이벤트 속성 → JS 에서 `addEventListener` / `style="…"` 속성 → CSS 클래스나 JS 의 `el.style.x`. `innerHTML` 템플릿 안도 같다. 어기면 **브라우저가 조용히 막는다**(버튼 무반응·스타일 누락, 게이트는 못 잡음)
- **외부 출처(CDN·SDK·폰트) 추가 = CSP 한 줄 추가** — 지금은 0 개(폰트도 시스템 폰트). 카카오맵 등을 붙일 때 `SecurityConfig` 에 그 지시어만 연다

### 모양

- `.editorconfig` 가 들여쓰기를 정한다 — 자바·XML 탭, JS·HTML·CSS 스페이스 4. 위임받은 코드는 붙여넣고 IDE 포맷 한 번

### 빌드

- **`-parameters` 컴파일 옵션 필수** — `build.gradle`과 IDE 양쪽 모두.
  Spring 7.0 / MyBatis 조합에서 이게 없으면 `@PathVariable`, `#{}` 바인딩이 실패함

### 실행 환경 (프로파일, ADR-042)

- 로컬은 `local`(STS Run Configuration 의 Profile 칸), EC2 는 `prod`(systemd 환경변수), CI 는 없음
- `application.yml` 은 항상 읽히고 `application-{프로파일}.yml` 이 그 위에 얹힌다 — 프로파일 파일엔 **공통과 다른 값만**
- 기본값(프로파일 없음)이 운영에 안전한 쪽 — 매퍼 `DEBUG` 로그는 `local` 에만 (운영 로그에 사용자 이메일·금액이 남지 않게)

### 비밀값 · 개인정보 (코드와 문서 같은 기준)

- **문서(md)도 코드의 환경변수 처리와 똑같이 관리한다** — 저장소에 올라가는 모든 파일엔 값이 아니라 **이름만** 적는다.
  코드가 `${DB_PASSWORD}` 라고 쓰듯, 문서도 `EC2_HOST`, `DB_PASSWORD` 처럼 이름으로만 가리킨다
  - 이유 — 저장소는 공개(포트폴리오)이고, git 이력은 지워도 남는다. 한 번 올라간 값은 "노출된 값"으로 보고 교체해야 한다
- 적지 않는 것 — 비밀번호·시크릿·API 키·SSH 키, 서버 IP·도메인 관리 계정, 실제 이메일·전화·주소, 실사용자 데이터
- 값이 있을 곳 — 로컬은 셸 환경변수 / CI·CD 는 GitHub Secrets / EC2 는 systemd `EnvironmentFile`(권한 600)
  - **비밀번호·키 원본은 저장소 밖 개인 보관처**(암호 관리처)에 둔다. 저장소 폴더는 gitignore 여도 실수로 풀리거나 통째로 복사될 수 있다
  - `docs/로컬환경.md`(gitignore)는 **값이 아니라 메모** — 테스트 계정, QA 실행법처럼 커밋하긴 그렇지만 비밀도 아닌 것
- **값 3단 분류 (2026-09-19)** — 새 값이 생기면 "비밀인가 → 서버를 가리키는가 → 둘 다 아닌가" 순서로 묻는다
  1. **비밀(자격 증명)** — 키·비번·토큰. 그것만으로 무언가가 열린다 → GitHub **Secrets** / EC2 `ledger.env`
  2. **식별자** — 서버 IP·AWS 계정 ID·보안그룹 ID·역할 ARN. 그것만으론 아무것도 안 열리지만 서버를 가리킨다 → **이 저장소에선 Secrets**
     - 이유 — **공개 저장소는 Actions 로그도 공개다.** GitHub Variables(`vars.`)는 로그에 그대로 찍히고 Secrets 는 `***` 로 가려진다.
       비공개 저장소였다면 Variables 가 제자리다 (실무의 기본형은 비밀 = Secrets / 비밀 아닌 설정 = Variables / 상수 = 코드)
  3. **공개돼도 되는 설정·상수** — 리전, 계정 이름(`deploy`), 포트, 서비스 이름 → **코드(yml)에 그대로.**
     환경이 하나뿐이라 Variables 로 뺄 이유가 없다 (YAGNI, ADR-042 의 "환경마다 다른가 + 비밀인가"). staging 이 생겨 환경이 둘이 되면 그때 Variables
- **비밀을 다루는 원칙** — 실무에서 그대로 쓰는 것들
  - **장기 키보다 단기 자격** — 만료 없는 액세스 키 대신 OIDC 임시 자격(1시간). 훔칠 것이 없는 쪽이 지키는 쪽보다 낫다
  - **키 하나는 한 가지 일만** — `ledger-admin`(사람·전권)과 `ledger-deploy`(CD·jar 교체와 재시작만)를 나눈 것처럼. 유출 피해가 그 키의 일에 갇힌다
  - **유출됐을 때 무엇을 갈아 끼우는지 사고 전에 적어 둔다** — 비밀·식별자 목록표는 [aws-인프라.md](docs/aws-인프라.md) (값 없이 이름·위치·교체 절차)
  - **워크플로** — `permissions:` 는 필요한 것만(`id-token: write`, `contents: read`) / 비밀을 `echo` 하지 않고 `set -x` 를 켜지 않는다 / 서드파티 액션은 버전 태그보다 커밋 SHA 고정이 안전(태그는 옮겨질 수 있다)
  - **안전망은 켜 둔다** — GitHub secret scanning + push protection (실수로 키를 커밋하면 push 가 막힌다)
- 예외 — CI 의 일회용 컨테이너 값(`ci.yml` 의 `ci_test_password` 등)과 가짜 테스트 계정(`test@test.com`). **운영 값은 이것들과 다르게** 만든다
- 트러블슈팅·ADR 에 로그나 명령어를 붙일 땐 값 부분을 `<EC2_HOST>` 처럼 가린다
- 검사 — 배포 관련 커밋 전에 설계-체크리스트 8번

---

## 협업 방식 (AI 활용)

- **직접 작성** — 판단이 들어가는 로직(검증, 설계, 알고리즘). 백엔드 코어 약 70%
- **위임** — 답이 하나인 기계적 변경(프론트 DOM·CSS, 보일러플레이트). 위임한 코드도 diff 검토
- **원칙** — 위임한 코드도 한 줄로 설명 못 하면 넘어가지 않는다
- 완성 코드를 통째로 받지 않고 **단계별 힌트를 받아 직접 작성한 뒤 검증**받는 방식
- 스프린트마다 "깊게 팔 영역"을 하나씩 지정
- 리뷰 — 기능 단위 QA + 스프린트 종료 시 전체 리뷰 1회. 발견 항목은 로드맵에 적고 즉시 고치지 않음
- **회귀는 CI 가 돌린다 (2026-09-20, ADR-042)** — main 에 push 하면 `build → qa → deploy`. `qa` 잡이 러너의 일회용 MySQL 8.4 에서 `qa/test-*.sh` 전부(지금 146케이스)를 돌리고, **하나라도 실패하면 배포되지 않는다**
  - 새 기능의 QA 스크립트는 `qa/test-<기능>.sh` 로 만들면 **파일 추가만으로 게이트에 들어간다**(글롭). 조건 — 첫 줄에서 `_lib.sh` 를 source, `ask_reset` 으로 시작, **마지막에 `summary`**(종료코드를 내는 곳), 전제 계정은 `reset-all.sh` 의 둘
  - 로컬에선 만드는 중인 기능의 스크립트만 돌려도 된다. 전체 회귀를 기억해서 돌릴 필요가 없어진 것이 게이트의 목적
  - 문서만 바꾼 push(`docs/**`, `**.md`)는 CI 자체가 안 돈다
- **YAGNI 우선** — 지금 필요한 것만 고치고 나머지는 해당 기능 만들 때. 고칠 목록이 길면 로드맵으로 잘라서 진행
- **배포·인프라는 예외 (2026-09-18)** — EC2·systemd·CD·Nginx·AWS 콘솔 같은 인프라 작업은 힌트 방식이 아니라 **AI 가 순서·명령·이유를 직접 안내하고, 나는 따라가며 질문한다.**
  이유 — 백엔드 코어는 판단이 들어가서 직접 써야 몸에 남지만, 인프라는 답이 정해진 관례가 대부분이라 힌트로 돌리면 시간만 든다.
  그래도 지키는 것 — 실행은 내가 맥 터미널에서 직접 하고 출력을 붙인다(pem·비번은 AI 에게 안 보여줌) / 경로·권한·계정 같은 결정은 "왜"를 같이 듣고 문서(진행상황·ADR·aws-인프라)에 남긴다 / 과금 클릭 전엔 설정 요약 확인

### 세션(대화방) 관리

대화가 길어지면 AI 가 앞부분을 요약본으로만 기억하게 되어 세부가 흐려진다.
**AI 가 먼저 알려줄 것** — 대화가 무거워졌다고 판단되면 작업 중간에 끼워넣지 말고,
한 단계가 끝난 지점(커밋 직후, QA 통과 직후)에서 "새 방 파는 게 좋겠다"고 말하고 아래 인수인계 메시지를 준다.

인수인계 메시지에 담을 것:

1. 직전 커밋 해시와 제목 (어디까지 커밋됐는지)
2. 지금 하던 작업 — 어느 단계, 어떤 파일, 무엇을 결정하다 멈췄는지
3. 미결정 사항 (사용자가 답해야 하는 질문)
4. 바로 다음 할 일 1~2개
5. 새 방 첫 메시지로 붙여넣을 수 있는 형태 (코드블록 하나로)

**기능을 시작할 때 AI 가 먼저 알려줄 것** — 새 기능(배포, 로그인 잠금, Sprint 2 …)의 첫 단계에서, 힌트를 주기 전에
`docs/진행상황.md` 의 "바꾸기 쉬운 구조 — 시점별" 항목 중 **그 기능 직전으로 적힌 것**을 꺼내 "이것부터 할지"를 묻는다 (ADR-041).
사용자가 묻지 않아도 꺼낸다. 끝낸 항목은 그 자리에서 체크한다.

**문서(md)와 AI 메모리는 항상 같은 내용으로 맞춘다 — 단, 수시로가 아니라 한 번에.**
- 정본은 저장소 docs. AI 메모리엔 문서 위치·공통 컨벤션·현재 단계만 둔다
- 동기화는 **마일스톤(단계 완료·커밋 직후)이나 "메모리 동기화" 요청 때** md 와 메모리를 **같은 턴에 동시에** 기록한다. 한쪽만 고치고 넘어가지 않는다
- 작업 중간에 매번 메모리를 고치지 않는다 (토큰 낭비 + 아직 안 굳은 결정이 메모리에 박힌다)
- **예외 — `aws-인프라.md` 의 자원 표·작업 이력은 단계마다 바로 적는다 (2026-09-19).** 콘솔 클릭은 이력이 안 남고 세션을 자주 바꾸므로, 마일스톤까지 미루면 "무엇을 만들었는지"가 대화방과 함께 사라진다. 메모리는 그대로 마일스톤 때
- 메모리에도 값(IP·키·비번)은 넣지 않는다 — "비밀값 · 개인정보" 규칙 그대로

새 방에서는 `CLAUDE.md` → `docs/진행상황.md` → `docs/decisions.md` 순으로 읽고 시작한다.
협업 방식(백엔드 코어는 단계별 힌트·직접 작성, 배포·인프라는 AI 안내·질문)은 이 파일에 적혀 있으므로 매번 설명하지 않아도 된다.
